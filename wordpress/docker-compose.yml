services:
  db:
    # Digest of the amd64 MariaDB image observed on the running server 2026-09-20.
    image: mariadb@sha256:07c0aaff7396b74cb7975cba78257178d188e30f531a5db2b617c48beef13c41
    container_name: wordpress_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:?Set MYSQL_ROOT_PASSWORD in wordpress/.env}"
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wordpress
      MYSQL_PASSWORD: "${MYSQL_PASSWORD:?Set MYSQL_PASSWORD in wordpress/.env}"
    volumes:
      - db_data:/var/lib/mysql
      - ./mysql_custom.cnf:/etc/mysql/conf.d/custom.cnf:ro

  wordpress:
    # WordPress 7.1.2 / PHP 8.3 Apache image. This digest was isolated-restore
    # tested against the 2026-09-26 v3 production backup before being pinned.
    image: wordpress@sha256:8ae73d594a154e30b112e8d085afbc0895acf7651d8f53ef63aefb42e30822d5
    container_name: wordpress_app
    restart: always
    depends_on:
      - db
    ports:
      # Match the live reverse-proxy deployment; do not bypass HTTPS on public port 80.
      - "127.0.0.1:8080:80"
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_NAME: wordpress
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: "${MYSQL_PASSWORD:?Set MYSQL_PASSWORD in wordpress/.env}"
    volumes:
      - wp_data:/var/www/html
      - ./uploads.ini:/usr/local/etc/php/conf.d/uploads.ini
      # Keep WP-CLI available across container recreation; the host file is
      # created by provision-wp-cli.sh from a versioned, checksum-pinned release.
      - ./wp-cli.phar:/usr/local/bin/wp:ro

volumes:
  db_data:
  wp_data:

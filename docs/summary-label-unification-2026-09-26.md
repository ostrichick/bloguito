# 생활정보24 요약 박스 제목 통일 완료 기록 (2026-09-26)

사용자 승인: 기존 모든 포스트의 초록색 요약 박스 제목과 앞으로 작성할 글의 기본 제목을 '한눈에 보기'로 통일한다.

## 적용 결과

| 상태 | 전체 조회 | 변경 | 변경 대상 없는 글 |
| --- | ---: | ---: | ---: |
| 공개 | 41 | 41 | 0 |
| 임시글 | 5 | 5 | 0 |
| 휴지통 | 8 | 4 | 4 |
| 합계 | 54 | 50 | 4 |

기존 요약 제목 잔존: 0개. 변경 제목은 '핵심 답변', '핵심요약', '핵심 요약', '먼저 답부터'였다. #345의 '3초 요약' 중복 배지 1개도 제거했다. 요약 박스가 없는 휴지통 4편과 요약이 아닌 초록색 본문 안내 박스는 변경하지 않았다.

변경 게시물 ID: 231, 465, 463, 470, 474, 235, 475, 471, 233, 466, 464, 239, 237, 241, 345, 229, 227, 393, 225, 349, 243, 304, 218, 219, 220, 217, 163, 145, 144, 140, 137, 139, 135, 81, 113, 112, 125, 101, 99, 127, 119, 121, 103, 105, 85, 79, 77, 70, 63, 55

## 검증된 보존 범위

최초 조회 원본 54편과 최종 운영 저장본 54편을 글별 대조했다. 요약 제목의 텍스트와 #345의 배지 태그 외 HTML은 완전히 동일하다. 글 제목, 상태, 발췌문, 슬러그, 작성일, 작성자를 보존했다. 최종 재실행 검사에서 모든 요약 제목은 '한눈에 보기'이며 추가 수정 대상은 0개다. 마지막 서버 배치에서는 GMT 작성일도 보존 검증했다.

새 글의 공통 렌더러 `agent-publisher/agents/editorial.py`와 공통 집필 지침 `docs/EDITORIAL_SYSTEM.md`를 로컬과 서버에 적용했다. 운영 서버의 실제 Python 환경에서 새 글 HTML을 생성해 '한눈에 보기' 출력을 확인했고, 지침의 고정 제목 규칙도 확인했다. 검증용 새 글을 WordPress에 등록하거나 공개하지 않았다. 2026-09-21 당시 구현을 서술한 역사 문단은 당시 표현을 보존한다.

비로그인 공개 화면 QA는 지정 UTM 첫 방문으로 시작했다. #231(최신 박스), #345(구형 박스)의 데스크톱 1440×900 및 모바일 390×844에서 제목이 한 번만 보이고 본문이 정상 줄바꿈되는 것을 화면 캡처로 확인했다.

## 안전한 적용 경로와 백업

`agents/summary_label.py`가 인식한 요약 제목 위치만 바꾼다. HTML 전체 재렌더링이나 본문 전체 문자열 치환을 하지 않는다. `scripts/normalize_summary_labels.py`는 최신 전체 목록, 원본 저장, 허용된 텍스트 위치 검증, 저장 결과 검사, 최종 전수 비교를 수행하는 전용 updater다. `summary_label_wp.php`는 행 잠금과 트랜잭션으로 원본 SHA 및 보존 필드 CAS를 검사한 뒤 원본 행을 백업하고 WordPress API로 갱신한다. 저장 검증이 실패하면 rollback한다. 실제 잘못된 SHA 요청이 거부됨을 확인했다.

초기 #231은 성공했으나 #465에서 WordPress가 draft 작성일을 바꾸는 기본 동작을 보존 검사로 탐지해 rollback했다. 운영 WordPress의 `wp_update_post()` 코드를 확인해 `edit_date=true`를 적용한 뒤 최신 목록에서 남은 49편을 재처리했고 모두 성공했다. 관리 연결의 일시적인 DNS/호스트키 조회 실패가 있어 재접속했으며 호스트키 검증을 끄지 않았다. 글별 왕복을 줄이기 위해 같은 전용 updater를 서버에서 한 번에 실행했다.

운영 서버 백업:
- 코드 원본 및 배포 manifest: `/home/ubuntu/agent-publisher/data/editorial_runs/summary-label-code-20260926T212510/`
- 배치 원본/최종/보고서: `/home/ubuntu/agent-publisher/data/editorial_runs/summary-label-20260926-final/`
- 변경 글 원본 행 JSON 50개: 위 경로의 `backend-backups/` (컨테이너 임시 폴더에서 호스트 영구 경로로 복사, 실제 파일 50개 확인)

로컬 원본/검증 데이터는 Git에서 제외한 `tmp/summary-label-20260926/`에 보관한다. 최초 54편의 `before.json`과 최종 전수 데이터/글별 해시는 `final/`에 있다. 사용자용 CSV는 모든 54편의 ID, 상태, 변경 여부, 변경 전후 제목 및 SHA256를 포함한다. 공개 상태를 바꾸는 발행 작업이나 삭제 작업은 수행하지 않았다.

## 코드 검증과 기존 작업 보존

관련 테스트 48개 통과. 최종 프로젝트의 전체 테스트 506개 실행, 505개 통과, 1개 skip, 오류 0개. PHP 구문 검사와 원본 불일치 CAS 거부도 확인했다. 격리 작업 공간의 구 기준 b8232f8에서는 기존 #85 테스트 실패가 원본 코드에서도 재현됐으나, 이후 최신 프로젝트 dd52e78에 이번 변경만 합쳐 전체 suite를 실행했을 때 모두 통과했다.

관리된 worktree 생성 도구는 이 projectless 채팅에서 'Not a git repository'를 반환했다. 따라서 실제 Bloguito Git 저장소에서 수동 worktree `codex/summary-label`을 생성해 격리했다. 시작 시 다른 작업의 미커밋 변경은 손대지 않았고, 그 변경이 다른 작업에서 커밋된 후 현재 프로젝트에 이번 차이만 적용했다. 별도 미추적 파일 4개는 해시를 대조해 보존했다. 커밋에는 이번 코드, 테스트, 현행 지침과 이 기록만 포함한다.

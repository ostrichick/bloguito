# Bloguito 기존 글 현대화 — 운영자 워크플로 E2E 독립 읽기 전용 감사 (2026-09-23)

## 판정과 실제 실행 범위

**2026-09-23 10:50~10:58 KST.** `AGENTS.md`, `docs/EDITORIAL_SYSTEM.md`, `docs/OPERATIONS.md`, 9/22 계획·9/23 실제 실행 기록, `docs/legacy-flu-postpublish-qa-2026-09-23.md` 및 `docs/legacy-operational-integrity-audit-2026-09-23.md`를 재확인했다. 독립 운영자 경로는 **현재 로컬 현황 화면 → 운영 WordPress 읽기 전용 전체 목록 → 대상별 원고·저장 SHA·출처 검사 → 정규 CLI 보류 → 비교 미리보기/manifest 확인** 순서로 재현했다. `PASS`는 해당 명령의 확인된 범위, `FAIL`은 확인된 기대 동작 결손, `UNVERIFIED`는 실제 계정·화면·행동 증거가 없음을 뜻한다. 인용하는 `tmp/` 자료는 Git 무시 경로이며 **`*.PRIVATE.json`과 stdout의 본문/초안은 공개 링크 또는 대시보드에 연결하지 않는다.**

| 단계 | 정확한 실제 결과 | 판정 |
| --- | --- | --- |
| 진단 화면 | `tmp/legacy_audit_20260923/prime/current-dashboard-postpublish.html`을 HTML로 열어 게시물 29행, 표 1개, 링크 38개, `form/button/input` 0개를 파싱. #63/#81/#140은 `최신형 본문 · 사실 검토 별도`이지만 09:14 이전 기준선 대비 `changed_public_post_candidate`를 아직 표시한다. 09:22 운영 재감사 이후의 신규 변경이라는 뜻이 아니다. 세 게시물의 후보 미리보기 링크는 없음. #85/#103의 비교 링크는 **9/22** `full-mixed` 사본을 가리키며 경로 자체는 존재하지만 오늘의 최신 리뷰·승인을 입증하지 않는다. 현황 화면은 **승인/게시 관리자 화면이 아니다.** | **PASS (정적 진단) / 승인 UI UNVERIFIED** |
| 현재 전체 목록 | 승인 준비기 `read_wp_json()`은 호스트·명령 화이트리스트를 검사하며 SSH로 `post list`의 `publish,draft,pending,future,private` 5개 상태를 조회한다. 10:5x 별도 독립 읽기에서 **39개 고유 ID=공개 29·초안 10**, #85 정확히 공개 한 건 확인. CLI 중복 검사용 목록은 자기 #85만 제외한 **38건**을 현재 다시 읽어 만들었다. 원격 다른 초안의 제목·본문·ID는 보고서/터미널/대시보드에 출력하지 않았고 전체 자체 제외 목록은 `inventory-excluding-85.PRIVATE.json`에만 저장했다. 6개 휴지통은 앞선 09:22 운영 감사에서 별도로 확인된 값이며 이번 `post list` 5개 상태에 포함되지 않는다. | **PASS (WP-CLI 조회 범위)** |
| 기발행 #63/#81/#140의 저장본 | 10:51 각 최신 검토 bundle을 원본으로 `scripts/prepare_post_approval.py --post-id ID --bundle ... --out-dir <각각 새 빈 개인 폴더>` 실행. 운영 목록+개별 `post get` 일치, 제목·slug·게시일 보존, 저장 `post_content` SHA가 이미 신규 원고 SHA와 **3/3 완전 일치**, 출처 즉시 재수집 SHA **3/3 일치**. 실제 비교 diff **모두 0줄**. 각 당시 CLI exit 0 / `preflight_status=ready`였지만 `approval_status=prepared_not_authorized`; **같은 공개 글을 또 갱신하거나 재승인하지 않았다.** 기존 공개 저장 SHA는 #63 `128f680d38734c1fcc4f7edcd40cf96e186632092665d9a3999330014568a541`, #81 `959d932cb488e8c85685ef37333478635ac6d0bb29d819985b469d83fbb71c3f`, #140 `9a0d8c8b1db3b1bd9fcf93bdde1781ce6bdb35fd97d38a9919c58df16404f1b1`. | **PASS (실저장·출처·무변경 확인); 새 승인 아님** |
| 검증 스키마·안전장치 변경 뒤 동일 재실행 | 작업 도중 prime이 별도 소유 파일에 내부 관련 글 링크 보존 가드와 새 편집 스키마를 반영했다. **10:52:55~10:53:15** 별도 새 빈 폴더 세 개에서 동일 원고로 세 건을 다시 `prepare` 실행. 각기 exit **2**, `preflight_status=blocked`, 사유 정확히 `review_not_bound_to_current_content` **한 건**; `source_hashes_match_live=true`, 저장=제안 SHA, diff=0, approval 미승인. 새 정책/스키마의 내용 서명과 옛 v3 리뷰 서명이 일치하지 않는 독립 차단이다. 옛 `ready` 기록으로 이후 새 적용을 승인하면 안 되며 **신규 적용하려면 현행 스키마의 새 독립 검토가 필요**하다. 이번 작업은 검토 호출·리뷰 서명 수정·재승인 없음. | **PASS (새 서명 게이트의 fail-closed); 기존 리뷰 재사용 FAIL** |
| 보류 #85의 실제 CLI | 9/23 `mixed2/post-85-service-guide-unreviewed.json`을 개인 무시 폴더로 별도 복사, 위 최신 전체 목록에서 자기만 제외한 PRIVATE 목록으로 `editorial_cli.py check <복사본> --inventory <실 WP 목록> --output <PRIVATE 보고>` 실행. **exit 2 / `needs_review`**, 사유 `dated_category_cannot_bypass_time_check`, `review_not_bound_to_current_content`, `review_stale`, `semantic_review_failed`. 기존 `welfare`+`evergreen` 정책 충돌이 선행 차단이며 해당 후보에는 독립 `review` 기록 자체가 없다. `review` 명령, 모델 API 호출, publish, update-existing, update-draft, promote는 실행하지 않았다. | **PASS (정규 CLI 차단)** |
| 보류 #85의 실제 비교 패키지 | 동일 복사본을 `prepare_post_approval.py --post-id 85 ... --out-dir <새 빈 폴더>`에 전달. **10:53:59** exit **2**, manifest `blocked`, CLI의 위 네 사유와 일치, 공식 출처 5건 즉시 재수집 SHA 일치. 기존 공개 원문 대비 제안은 **60줄 diff**, 비교 `compare-preview.html`·`content-diff.txt`·`proposed-reviewed.html`와 PUBLIC 글 원본의 `post-original.PRIVATE.json`은 로컬 전용으로 생성됐지만 `approval_status=prepared_not_authorized`, `wordpress_write_performed=false`. 미리보기가 있다는 사실은 게시 가능한 상태 또는 승인에 해당하지 않는다. | **PASS (보류 원인·미리보기 생성), 실제 적용 BLOCK** |
| 비교 HTML·원본 보안 | 총 7개 독립 패키지(첫 기발행 3, 후속 가드 3, #85 한 건)의 `post-original.PRIVATE.json` 파일 실제 바이트 SHA와 manifest 기록 **7/7 일치**. 모든 비교 HTML은 `미적용` 제목 및 `iframe sandbox="" srcdoc`를 갖고, 임의 PRIVATE 파일명을 포함하거나 원본 파일로 연결하지 않음. manifest 7/7 `prepared_not_authorized`, `wordpress_write_performed=false`, `rollback_not_executed=true`. 이 결과는 **HTML 정적·파일 SHA** 검증이지 실제 브라우저 렌더링/Windows ACL 증명은 아님. 특히 이미 게시된 세 글의 비교 화면도 무조건 `미적용`이라고 표기하는 UX는 현재 상황에 맞지 않는다. | **PASS (격리·SHA), 기발행 미리보기 상태 라벨 FAIL/혼동 위험** |

## 검증 중 발견한 중요 링크 회귀와 안전장치의 적용 범위

**#81 관련 글 내비게이션 FAIL:** 9/22~9/23 개정 직전 #81의 공개 게시물 원본 백업에는 `독감 예방접종 전체 일정`이라는 내부 링크 `https://lifeinfo24.org/?p=63`이 있다. 실제 공개된 v3 #81에서는 이 링크가 사라졌다. 이전 원본과 지금 공개 글을 직접 비교하면 **누락된 내부 게시물 ID는 `[63]`**. 본문의 공식 예방접종기관 CTA가 작동한다고 해도 이 별도 관련 글 연결 복원 여부까지 인증하지 못한다. 구 v3 원고를 다시 적용하는 행위로는 누락이 복구되지 않는다.

prime의 신규 `agents.related_links.missing_internal_post_ids()`는 원본과 제안 원고의 **동일 사이트의 명시적 `?p=<정수ID>` 링크** 차집합을 계산하며 승인 준비기와 실제 updater 양쪽에 연결돼 있다. 하지만 새 검사는 실시간 **현재 저장본**과 제안본을 비교한다. 10:53의 #81은 **이미 회귀된 실서버 현재 본문 == 이미 회귀된 제안 본문**이어서 `lost_internal_post_ids=[]`가 나왔다. 별도 역사적 공개 원본과 현재 본문에 함수를 직접 적용한 결과는 `[63]`, 현재→현재는 `[]`. 증거: `operator-e2e/nav-guard-probe-summary.json`, `postguard-81/approval-manifest.json`. 따라서 가드는 **이후 새로운 삭제는 차단할 수 있어도 이미 게시된 링크 손실을 소급 감지하거나 자동 복구하지 않는다.** 역사적 승인 원본 대조 또는 명시적 관련 링크가 포함된 새로운 근거/리뷰 원고가 필요하다. 새 가드의 모든 일반 내부 URL 형식 커버리지까지 검증한 것은 아니다.

**#140 참고:** 과거 원본의 정부24 링크는 개별 신청 양식이 아니라 일반 홈페이지 `https://www.gov.kr/`로 확인됐다. 개정본 #140은 CTA 0개이며 해당 일반 링크가 제거됐다. 이 사실 자체를 곧바로 새 신청 기능의 결함이라고 하거나 미검증 일반 홈페이지 버튼을 자동 복원하지 않는다. 실제 신청·처리 상태의 고유 목적지를 확인하는 별도 작업이 필요하다.

## 명령 권한 경계와 관리자 UI 장애

`guard_smoke.py`의 **5개 음성 테스트 전부 PASS**: 잘못된 대상 ID의 bundle, 이미 파일이 든 출력 폴더, Git 무시 `tmp/` 바깥의 출력, WP 조회 함수에 전달한 수정 명령, 쉘 메타문자가 들어간 SSH 호스트를 각기 DB 접속·원격 변경 없이 `ValueError` 또는 `FileExistsError`로 거부했다. 승인 준비기의 CLI 전체 동작은 `wp post list/get` 화이트리스트 외에 명령을 전달할 수 없다. 업데이터는 **호출하지 않고 코드만 열람**했으며 명시 `confirmed`, 대상 ID, 64자 이전 저장 SHA, 락, 자기제외 전체 인벤토리, 의미 리뷰/공식 출처 해시, 쓰기 직전 CAS, 백업·저장 후 일치 경로를 포함한다. 코드 존재와 운영에서의 고장 없는 전체 쓰기 재현을 동일시하지 않는다.

**WordPress 인증 관리자 화면 E2E: UNVERIFIED.** 실행 브라우저의 디버깅 포트는 **9241 한 개**로 관찰됐지만 worker-1이 소유한 기존 공개 독자 QA 세션이므로 접속·탭 나열·내비게이션을 하지 않았다. 별도 소유권이 확인된 로그인 관리자 탭/계정은 제공되지 않았으며 도구에서 확인하지 못했다. 새 브라우저·로그인·관리 화면 편집·저장·출력을 시험하지 않았다. 이전에 설치된 관리자 ID 열 MU `bloguito-post-id-column.php`의 실제 서버 파일 SHA `6e3c383122341d37c0a1921a4c0cc62bf1d82b964775e52b459f2d478d6d47cd`가 9/21 기록과 **현재도 일치**함은 별도 읽기 전용 SSH로 확인했으나, 이 파일 지문은 실제 로그인 화면에 ID가 보이는지 인증하지 않는다. 관리자 UI E2E는 **소유자가 분리된 인증 세션이 제공될 때** `글→모든 글→ID 열/상태/편집 화면의 읽기 전용 열람`으로 재시험해야 하며 저장 버튼·신청 제출·게시·승인 실행 없이 끝내야 한다.

## 안전한 운영자 재현 절차와 인계

1. 해당 날짜의 **새 완전 공개 REST 감사**를 수행하고 진단 화면은 비공개 승인도구가 아닌 색인으로만 사용한다. 위 화면의 #85/#103 기존 비교는 9/22 후보임을 명시해야 하며 `changed_public_post_candidate`는 비교 기준점에 종속된다. 운영 전체 상태 목록은 별도 `post list/get`으로 다시 조회하고 비공개 본문을 콘솔/HTML에 노출하지 않는다.
2. 승인 준비 대상이 이미 공개된 현재 원고인지 먼저 **원본 DB SHA==제안 SHA / diff 0**으로 구별한다. 이미 갱신된 #63/#81/#140에는 재적용 명령을 내리지 않는다. 새로운 출처·검토 스키마 변경이 있으면 옛 검토 서명 `ready`를 재사용하지 않는다. 개정 전 원본과 현재 글의 관련 링크 차이도 별도로 비교한다.
3. 보류 #85는 검토 전 `check` 및 `prepare`에서 실제 `needs_review/blocked`를 확인했으므로 Preview 파일은 증거일 뿐 승인/적용 기능이 아니다. 형식 통과를 위해 welfare를 다른 분야로 위장하거나 마감일을 만들지 않는다. #103은 공식 15~65/15~64세 충돌과 기관 대상 범위가 별도 수동 HOLD이므로, 해당 결손을 자체 해결했다고 보고하지 않는다.

**이 회차 소유 산출물:** 이 보고서 한 개와 Git 무시 `tmp/legacy_audit_20260923/operator-e2e/`만 새로 작성했다. 직접 실행 증거는 7개 각 폴더의 `approval-manifest.json` 및 PRIVATE 로그, #85 `post85-cli-check.PRIVATE.json`, `preview-smoke-summary.json`(7/7), `guard-smoke-results.txt`(5/5), `nav-guard-probe-summary.json`에 있다. 이전 QA와 독립 ops 문서 및 prime의 수정 중 코드·원고/승인 패키지를 고치지 않았다. **운영 WP/서버/cron 쓰기, 승인·재검토 모델 호출, 게시물 재적용, Git stage/commit/push는 0건.** 실제 admin UI, worker-1 브라우저, 네이티브 확대/보조기술, 신청·저장·출력, 개인정보/백업 복구 E2E는 **미검증**으로 남긴다.

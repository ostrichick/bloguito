# 구형 공개 글 16편 독립 콘텐츠 회귀·편집 게이트 감사 — 2026-09-23

## 판정과 실행 경계

**게시 완료 3편(#63·#81·#140): 현재 검토 원고/운영 저장 본문 일치, 정책·리뷰 게이트 PASS. 나머지 13편: 정규 `editorial_cli.py check` 13/13 차단(종료 코드 2), 운영 반영 승인 대상 0편.** 특히 #103은 리뷰 전 형식검사만 `ready`이나 공식 공지 상충·원자료 재수집 실패로 실제 **HOLD**다. `ready`는 원문의 사실 여부를 보증하지 않는다. 대상은 9/23 원래 구형 16편이며, 9/22 이미 개정된 11편은 별도 회귀 표본이다. 기존 2026-09-22 문서의 `ready`·미배포 기술을 현재 진실로 취급하지 않았다.

정본은 `AGENTS.md`, `docs/EDITORIAL_SYSTEM.md`, `agent-publisher/editorial_policy.json`과 현행 코드다. **최소 잔여 유효기간 30일**, 현재 정책 예외는 **#225 한 건만**이다. 공연·복지는 단지 신청 안내라는 이유로 `evergreen`으로 바꿀 수 없고, 출처 `fetched_at` 최신성은 정책의 적용 연도·기간 증빙과 다르다. 13 보류 원고에 임의 날짜 연장·기간 예외·공식 URL 가짜 수집·검토서명 복사를 하지 않았다. 이 감사에서는 WordPress 글/서버/MU·공통 코드·타 작업자 파일·Git stage/commit/push 모두 변경하지 않았다.

검증 증거는 **Git 제외** `tmp/legacy_audit_20260923/content-qa/`에만 저장했다. `audit.py`는 allowlist 읽기 전용 SSH `post list`로 5개 활성 상태 전체 인벤토리 39건(공개 29·초안 10, ID 중복 0)을 수집하고 글별 자기 ID만 제외한 38건을 검사에 제공했다. 전체/글별 인벤토리는 `*.PRIVATE.json`으로 별도 저장하고 0600 모드로 제한한다. `audit-results.json`에는 다른 임시글의 비공개 제목·본문을 노출하지 않고 중복 ID 중 공개만 표시한다. 각 번들의 공식 URL·인용, 소스 text 해시, 글 제목, 기존 WP 저장 SHA·렌더 SHA·6항목 리뷰 결합 및 전후 텍스트를 검사했다. **정규 CLI를 실제 13회** `check bundle --inventory <자기 제외한 9/23 재고>`로 실행했고, 입력 JSON 파일 해시 변경 0, 결과는 별도의 `validate_bundle(..., require_review=True)` 13/13과 일치했다. 검토가 결여된 후보에 `review`를 새로 만들어 붙이지 않았다.

별도 `refetch.py`는 현행 `fetch_sources`를 변경 없이 사용해 16편이 참조하는 **공식 URL 고유 33개**를 실제 독립 재조회했다. 파서 성공 **31개**, 공식 본문 SHA 일치 **21개**, 성공했으나 SHA 변동 **10개**, 재수집 실패 **2개**(#70 NOL 403·#103 HWP 파서 실패). 동일 공식 URL을 여러 글이 공유하면 고유 URL 한 번의 검증으로 집계했다. 16편의 *보존된* sources `sha256 == SHA256(text)`와 모든 본문·표 행·FAQ answer 연결 인용의 원문 포함 검사는 16/16, 누락 **0건**이나, 이것은 10개의 현재 SHA 변동이나 공식 문서끼리의 논리 모순을 무효화하지 않는다. 출력 `official-source-refetch.json`에 URL별 결과가 있다. 공공기관의 가변 조회수·추천기사·가격 변화·첨부 메타가 원인일 수 있으나 **10건 모두 본문 불변이라고 실증한 것은 아니다**. 출처별 의미 차이 재대조 또는 정확 URL·DOM에 한정한 정규화 전에는 승인하지 않는다.

## 대상별 독립 검사 결과: 16편 전체

아래 `사전검사`는 현행 정책과 자기 제외 39건 인벤토리로 **리뷰 전** 검사한 결과다. `check`에는 별도로 리뷰서명/기한을 반드시 요구한다. `출처`는 오늘의 `fetch_sources` 동일 SHA 수/해당 URL 수이며, 불일치는 최신 원문을 무시할 수 없다는 뜻이다.

| ID | 사전검사 및 실제 정규 `check` | 당일 공식 재조회·구체적인 보류/품질 상태 | 판정 |
| --- | --- | --- | --- |
| **63** | 사전검사 및 리뷰 포함 `ready`, 6항목 PASS, digest·정책 digest·24h 유효; 저장 본문 = 최종 `render` 바이트 완전 일치 | KDCA 공식 2/2 SHA 일치, 최신 수정 일정과 시즌 원고 보존. 공개 REST HTML 및 상태 검사 별도 PASS | 게시 1/3 PASS |
| **81** | #63과 동일한 6항목 리뷰·정책·본문 해시 PASS | KDCA 공식 2/2 일치, 65세 이상 차등 개시 안내를 포함한 현재 원고와 운영본 완전 일치 | 게시 2/3 PASS |
| **140** | 동일하게 리뷰·digest·정책·본문 해시 PASS, 보류 원고 아님 | 외교부 공식 1/1 일치. 18세 미만 법정대리인 온라인 신청·대리 수령 예외, 기존 여권/신분증·수령기관 변경 불가 근거 검토 본문 일치 | 게시 3/3 PASS |
| **70** | `availability_not_verified`, `temporal_source_not_bound`; CLI 2+리뷰 부재 | NOL 공식 상품 직접 재수집 **HTTP 403**(0/1). 어제 수동 발췌 SHA가 맞아도 실시간 재현 증거 아님. 공연일을 판매 종료일로 조작 금지 | HOLD |
| **85** | `dated_category_cannot_bypass_time_check`; CLI 2+리뷰 부재 | 정부24·복지로 5/5 출처 SHA 재조회 일치. 정부24 혜택알리미·복지멤버십 가입·급여 신청·신청 현황 메뉴 분리는 개선했지만 `welfare/evergreen` 자체 차단. 별도 9/21 서비스 중단 공지와 실제 실행 기능의 현행 확인 필요 | HOLD |
| **99** | `availability_not_verified`, `number_without_evidence`, `temporal_source_not_bound`; CLI 2+리뷰 부재 | 한국 NOL 403 대체 동일 상품번호 NOL World 영문 1/1 재조회 일치. 판매기간 종료·회차별 시각/좌석·휠체어/배송 근거는 새 영문 페이지에 없어 후보에서 삭제. 영문 `Nov`·`September`에 대응하는 한글 11월·9월 숫자 보수 검사 세 문단 차단 | HOLD |
| **103** | **사전검사 `ready`/사유 0**이나 리뷰 결합 실패 3종으로 CLI 2; 의미 검토 0 | KDCA·목포 어린이 3/5 일치, 시 보건소 HTML SHA 변경, **HWP 원본은 추출기 실패**. 9/16 보건소 공지 **15~65세**와 9/18 시 보도자료 **15~64세** 상충 계속 존재. HWP 81곳은 국가사업 목록이고, 어린이 웹의 6곳 표본은 전체 어린이 기관 아님. 87개 표 행의 정확 출처 범위·표 의미 검토 미완료 | **수동 필수 HOLD** |
| **119** | 잔여 7일로 `insufficient_useful_lifetime` + `availability_not_verified`, `temporal_source_not_bound`; CLI 2 | 서울시 2개 URL 모두 SHA 변동, K-패스 1건 일치. 전환시한은 9/29~30, 조회수/추천 기사 변경이라도 현 상태 해시 재생성·정확한 정책 본문 대조 필요. #225 예외 재사용 금지 | HOLD |
| **125** | `duplicate_topic`, `tax_points_2026_authoritative_version_missing`, `tax_points_2026_expiry_cohort_missing`; CLI 2 | 국세청 0/1 SHA 변동. 기사 원문 ’25 표기를 검증기가 `2025`로만 요구해 실제 원문과 불일치하는 **좁은 검증기 설계 충돌**이 있으나 수기 치환해 통과 처리 금지. 공개 #139 + 비공개 1건과 주제 중복, 법인/개인 만료 코호트 독립 의미검토 필요 | HOLD |
| **139** | `duplicate_topic`; CLI 2 | 4/5 출처 일치, 국세청 URL SHA 변동. 공개 #125·#220·#243 및 비공개 1건과 중복. **새 후보 제목 ≠ 현재 WordPress 제목**이므로 공통 `prepare_post_approval.py`의 제목 보존 게이트도 통과 불가; `validate_bundle`에는 현재 WP 메타 입력이 없어 이 사유가 나타나지 않는 정상 분업 | HOLD |
| **144** | 9/30까지 잔여 7일, `insufficient_useful_lifetime`, `availability_not_verified`, `temporal_source_not_bound`; CLI 2 | 서초구 세금 안내 SHA 0/1 변동. 서울 ETAX 분납 경로를 전국 공통으로 일반화하지 않고 실제 납부 마감 이후 현재형 제거 필요 | HOLD |
| **145** | 9/27까지 잔여 4일, 위와 동일 3사유; CLI 2 | 공식 PDF 1/1 일치. 9/23 특별교통대책과 9/24~27 통행료 면제는 다른 기간; 경계 진입·진출/민자 구간을 근거 없이 합치지 않음 | HOLD |
| **163** | 9/27까지 잔여 4일, 동일 3사유; CLI 2 | 보건복지부 기사 0/1 SHA 변동. 연휴 전국 **사전 계획**·개별 기관의 당일 운영·응급 119 범위를 구별해야 함. 기간 예외 없음 | HOLD |
| **217** | 9/27까지 잔여 4일, 동일 3사유; CLI 2 | 코레일 1/2 일치. 잔여석 9/11 **판매 개시일** ≠ 실시간 좌석 존재, 결제 완료 증거 없음 | HOLD |
| **219** | 9/27까지 잔여 4일, 동일 3사유; CLI 2 | 3/5 일치, 오피넷 원문(실제 유가)·정책브리핑 한 곳 SHA 변동. 충전 재고/가격/무료 이벤트를 현재 고정값으로 쓰지 말고 근거 있는 조회 동선만 고려 | HOLD |
| **220** | `availability_not_verified`, `temporal_source_not_bound`; CLI 2 | 1/2 일치, MOHW 공식 글 첨부 다운로드·미리보기 수 변동. 신청 기간과 종료일의 공식 인용 없음. 과거 진료연도 금액을 2026년 진료비로 오해시키지 않으며 에버그린 임의 변경 금지 | HOLD |

각 13편의 리뷰 기록은 모두 부재하여 리뷰 digest·정책 digest·기한·6/6·issues=[] 다섯 조건을 충족하지 못한다. 그러므로 모든 CLI 결과에는 `review_not_bound_to_current_content`, `review_stale`, `semantic_review_failed`도 포함된다. **0/13 전자 결재·적용 준비**. `audit-results.json` 각 행의 `preflight_no_review`, `preflight_with_review`, `cli_report`, `cli_exit_code`와 `input_unchanged_by_cli`가 정확한 원본이다. 정책 예외 `dated_post_exception`은 16편 모두 미적용, 자기 제외 전체 WP inventory에서 명백한 중복은 #125와 #139 두 편이다.

## 사실 주장 및 대체 원고 손상 위험 (규칙 검사만으로 PASS 불가)

1. **#103이 우선 결함 사례:** 9/16 목포시 보건소 [공식 공지](https://www.mokpo.go.kr/health/citizen_participation/notice?idx=548678&mode=view)와 9/18 [목포시 공식 보도자료](https://www.mokpo.go.kr/mayor/communication/news?idx=548751&mode=view)가 서로 다른 자체사업 상한(65 vs 64)을 게시했다. 새로운 HWP 명단은 국가접종 기관만 설명하고 이 자체사업 상한을 해결하지 않는다. 원고가 직접 숫자를 반복하지 않더라도 **공식 충돌을 삭제해 해결된 것으로 볼 수 없다**. HWP를 텍스트 source로 손수 보관하여 자체 해시와 인용검사는 통과하지만 표 81행의 원파일 추출·버전 재검사와 공식 서면 정정 없이는 승인 불가. 특히 전후 diff에 보이는 ‘전남광주통합특별시’ 등 원자료의 지역 표기를 작업자가 일반화하거나 비공식 주소로 바꾸지 않는다.
2. **#70·#85·#99 콘텐츠 축소:** 실제 저장 WordPress 본문을 파싱한 텍스트량은 순서대로 **1307→682, 2321→1185, 1259→635자**(약 절반)다. 분량 자체가 품질 판정은 아니나 전체 교체 시 중요한 기능 유실 가능성이 명확하다. #70은 기존 회차 선택·주문 완료 확인·9/15 오픈 FAQ가 제거됐고 할인·배송이 추가됐다. 다만 최신 **403 공식 문서가 재현되기 전에는 기존 문장이나 새 할인도 현행 확정으로 복원해서는 안 됨**. #99는 원문에 있던 각 회차 **18시/17시**, 전화 휠체어석·본인인증·입금·배송 조건이 World 공식에서 읽히지 않아 빠졌으며, 새 글의 핵심 질문을 공연 일정/요금/오픈으로 바꿨다. 이처럼 유실 사실과 근거 미충족을 별도로 공개 직전 검토해야 한다. #85는 기존 사업별 **대상·소득·재산·중복·신청일/시각·접수번호** 5행 표, 개인별 정리 양식, 온라인 불가 시 주민센터 경로가 사라졌다. 새 서비스별 메뉴 구분과 채널 변경은 유용하지만 기존 ‘조회→공고 조건→접수 결과’ 과제를 완료했다고 보기는 어려우며, 개별 사업 미지정 시 적용 불가 부분을 명시적으로 범위 조정해야 한다.
3. **#85 최근 서비스 가용성:** 복지로 9/21 [공식 신청 중단 공지](https://www.bokjiro.go.kr/ssis-tbu/cms/pc/customer/notice/1310378_1141.html)는 9/27 13:00~9/30 15:00 관할 지자체 신청서 전송 중단, 별도 로그인 중단 구간 2개를 구분한다. 이는 기존 5개 소스의 서비스 메뉴 안내나 개별 신청서 **현재 접수·즉시 전송**의 증거가 아니다. 일정을 본문의 영구 신청 종료일·에버그린 근거로 쓰면 안 된다. 복지로 동적 신청 URL이 HTTP 200이라도 실사용 양식이 공백이면 CTA를 활성 업무 경로로 승격하지 않는다. 조회·복지멤버십 가입·개별 신청·신청현황의 고유 목적지 접근/인증 전 범위에 대한 검증이 남았다.
4. **#125·#139·#220 의미 범위:** #125는 공식 원문 문자 ’25, `critical_facts.py`는 2025만 찾는 불일치가 실제 코드에서 재현된다. #139은 세금포인트가 ‘5번째 현금 환급금’이 아니라 비현금 혜택인 것은 유지했지만 기관별 4종 조회가 기존 #125/#220/#243과 중복되어 검색 과제가 분리되지 않았다. #220의 게시 근거상 신청 종료일 자체 미발견을 `useful_until`에 진료연도·추정 안내기간으로 대신할 수 없다.
5. **인용 형식 검증의 한계 (재현 가능한 합성 회귀):** `guard_probe.py`는 게시 완료 #140 번들을 **메모리 복사만** 하여 첫 섹션 제목을 ‘만 18세 미만은 전부 온라인 여권 재발급 신청 불가’로 변조했다. 실제 외교부 source는 18세 미만 법정대리인 정부24 신청을 허용한다. `validate_bundle(..., require_review=False)`는 변조 전후 **모두 ready**, 리뷰 요구 시 `review_not_bound_to_current_content`로 차단했다. 숫자·인용 검사는 제목·소제목·FAQ 질문을 본문 블록만큼 검사하지 않으며, 인용 안에 18이 있어도 **‘가능’↔‘불가’ 의미 반전을 기계적 숫자검사로 해결할 수 없다**. 이 입력은 합성 음성 테스트이며 실제 #140 게시물이나 검토 원고를 수정하지 않았다.

## 운영 회귀: 이미 반영된 14편과 #85 출력 MU

- 9/23 현재 **공개 #63/#81/#140 3/3**은 별도 검토 bundle의 `render(plan,sources)`와 실제 인증 읽기 전용 WordPress 저장 `post_content`가 바이트 단위 동일, 원본 해시·현재 제목·공개 상태 일치. 세 건 리뷰 모두 6개 체크 true/issues 빈 배열, 본문 digest·현행 정책 digest 일치, 유효기간 24h 이내; 실제 리뷰 포함 검증 `ready`/이유 빈 배열. 당일 KDCA 2/2, 외교부 1/1 공식 source 동일 SHA. 이것은 개인 접종 재고나 여권 신청 완료가 아니다.
- **9/22 개정 11편 #55/#79/#101/#105/#113/#121/#127/#137/#218/#243/#349의 현행 WP 저장 `post_content` SHA가 전날 작업 장부의 글별 최종 SHA와 각각 11/11 일치**. 기준: `docs/legacy-modernization-resume-execution-2026-09-22.md`의 갱신 결과 및 #218은 `docs/interruption-final-2026-09-22.md`의 최종 `2bf12...`. 새 재발행·업데이트는 0회. 날짜별 성과 문서만으로 새 사실/실행 URL이 오늘도 검증됐다는 뜻은 아니다. 특히 #127 홈택스 버튼이 실제 브라우저에서 메인으로 이동하는 잔여 문제와 #304의 별도 공식 정보 충돌은 별개이다.
- 익명 공개 웹 QA를 규정 UTM 첫 방문(HTTP 200)으로 시작하고 #85 공개 글(HTTP 200)의 **실제 응답 HTML**을 파싱했다. 목차 1개/앵커 5개 모두 존재, 표 정확히 2개, caption 2개, 열머리글 6개, 행머리글 7개, 각각 `role=region`·`tabindex=0`. 읽기 전용 서버 `sha256sum`의 운영 `bloguito-legacy-table-accessibility.php` SHA **`dace4a9fe30b78260081bb68a2a5706c7ff6019a389f74c32aee7fc80c21caad`** = 로컬 파일/9/22 최종 기록. #85 **저장 본문 SHA `499afcf30df2cfbfdca7041e80bc139c90e1a8da5d5348837b77ee2b8e2cf065`는 그대로**여서 표 MU 적용을 내용 개정으로 기록하면 안 된다. 오늘 네이티브 200%/스크린리더/모바일 키보드 실조작은 미수행이고 9/22 해당 QA는 당시의 한정 결과로만 인용한다.
- 추가 익명 WordPress REST 29건 전체를 09:14:51 KST의 **완전한 공개 `triage.json` 29건과 다시 비교**했다. ID 집합 일치, 수정시각·공개 `content.rendered` SHA 변경 **0/29**. 이는 익명 공개 REST 응답의 당시 일치이며, 서버의 가변 목차·MU 적용과 타 서비스 외부 원문까지 보증하지 않는다.

## prime에 제안할 정확한 좁은 변경 — 이 작업자는 코드 변경 없음

1. **#103 공식 연령 상충 의미 차단:** `critical_facts.py`에서 `existing_post_id == 103`, `category_key == 'life-health'`, 고정된 목포 보건소 공지 URL과 목포시 보도자료 URL이 모두 현재 sources에 있을 때에만 원문 `'15~65세'` vs `'15~64세'` 상충을 검사하고 `mokpo_city_program_age_conflict_unresolved`를 반환. 실제 공식 정정 URL·동일 적용집단이 제시되면 별도 사실·출처 검토 후 업데이트; 숫자 하나를 고치거나 둘 중 한 출처를 제거해 게이트를 통과시키지 않는다. 합성 테스트로 상충/동일/출처 누락/다른 글 제외를 검사. 이것은 **현재 `fetch_sources`가 HWP를 못 읽는 독립 차단을 대체하거나 자동 통과시키지 않는다**.
2. **#125의 공식 축약 연도 인식:** 정확한 NTS 세금포인트 안내 source URL/기관의 정책 본문에서만 공식 `’25년`/`'25년` 표기를 `2025년`과 동일 코호트 증거로 정규화하는 **버전 근거 한정 파서**를 제안한다. `critical_facts.py`의 단순 하드코딩 `2025` 완화만으로 다른 문맥이나 자료의 2025를 인정해서는 안 되며, 법인/개인 소멸 대상·5년·사용처 표의 실제 2026년 기준과 중복 판정 및 신규 리뷰는 여전히 독립 필수다. 같은 원문이 아닌 24년/26년·코호트가 다른 자료·중복 후보에 대한 부정 테스트를 추가한다.
3. **소제목 의미 회귀 포착:** 위 #140 합성 변조는 독립 모델 검토 결합이 실제로 차단하지만 *리뷰 전* `ready`가 사람에게 사실 완결로 오인될 여지가 있다. 새 정규 검토 프롬프트/테스트에서 제목·소제목·표 머리글·FAQ 질문까지 출처와 일치하는지를 문장 및 표 행과 동등하게 엄격히 검사하고, 사람 승인 자료에서는 `preflight_no_review=ready`를 **최종 ready**로 표기하지 말 것. 특정 위험표현(`18세 미만 전부 온라인 불가`)은 #140의 공식 미성년 예외와 배타적일 때만 좁은 사실검사 추가를 검토할 수 있다. 전 범용 자연어 의미 검사나 출처·30일·리뷰 게이트 비활성화는 제안하지 않는다.
4. **원문 SHA 잡음의 국소 개선 선택지:** `nts.go.kr` 세금포인트 특정 기사·MOHW `list_no=1491727` 첨부 조회수·목포 특정 공지의 갱신일·코레일/서초구의 카운터는 반드시 **개별 URL별 DOM 차이와 기사 본문 보존을 먼저 증명**한 후, 변동 카운터 요소만 정규화하고 '본문·표·첨부 내용 변화 시 SHA 변경' 부정 테스트를 붙인다. 오피넷 실시간 가격이나 실제 행사/정책 갱신까지 제거해 항상 같게 만드는 패치는 금지. NOL 403·HWP 읽기 실패는 임의 스냅샷 승인이나 외부 HTML 가짜 성공으로 대체 금지.

**남은 최종 결재 기준:** 이 문서 자체가 13편의 발행 허가가 아니다. 공식 상충/기간/중복/원문 최신성/콘텐츠 손상을 각각 해결하고 원래 WordPress 저장본·제목·slug·게시상태·전후 전체 본문·모든 출처 SHA와 정규 독립 모델 리뷰 6/6을 당일 다시 검증한 글만 글별 승인 후보로 올릴 수 있다. 별도 승인 뒤 정규 updater로 적용하고 공개 실브라우저 360/390px·네이티브 200%·키보드·링크 대상까지 확인해야 한다. 민감한 전체 WordPress 목록, 백업/접속 비밀은 보고서에 포함하지 않았다.

# 2026 기초연금 #101 보강 — 2026-09-24

## 요청과 기준선

- 사용자 요청: 공개 글 #101의 내용을 보강하고, 특히 수급자격 판단에 중요한 금액을 굵게 강조한다.
- 변경 전 WordPress: `publish`, 본문 SHA256 `8a2164194215f35fb1ae9f5914d97fac5d278f730e26f1f1981a2a258335334c`, 수정시각 `2026-09-22 16:12:43`.
- 기존 제목과 slug는 유지했다.
- 작업 시작 시 다른 업무의 `docs/INDEX.md` 및 기존 미추적 문서들은 본 작업 소유가 아니므로 수정·스테이징하지 않았다.

## 공식 근거

정규 `editorial_cli.py sources` 경로로 아래 다섯 개의 공식 자료를 2026-09-24에 새로 수집했다.

1. 보건복지부 2026 기초연금 선정기준액 발표
2. 국민연금공단 기초연금 수급 요건·신청방법
3. 보건복지부 기초연금 기준연금액 안내
4. 보건복지부 기초연금 소득인정액 산정방식
5. 보건복지부 기초연금 신청방법·준비서류

핵심 공식 기준은 단독가구 월 247만 원, 부부가구 월 395만 2,000원, 2026년 기준연금액 월 349,700원이며, 부부 동시 수급 시 각각 20% 감액될 수 있다. 선정기준액은 월급 자체가 아니라 소득·재산을 합산한 소득인정액 기준이다.

## 보강 내용과 포맷

- 상단 핵심 답변에 수급자격 기준과 지급 기준을 명확히 분리했다.
- `단독가구 월 247만 원`, `부부가구 월 395만 2,000원`, `월 349,700원`을 본문 `<strong>` 강조로 반복 노출했다.
- 상단 `2026년 기초연금 자격·지급 판단에 중요한 금액` 표를 추가해 4개 핵심 금액을 한눈에 보이게 했다.
- 대상자 조건: 만 65세 이상, 대한민국 국적·국내 거주, 소득인정액 기준, 직역연금 원칙적 제외를 보강했다.
- `선정기준액`과 `실제 지급액`을 별도 절로 분리해 오해를 막았다.
- 소득인정액에 근로·연금소득뿐 아니라 일반재산·금융재산·부채가 들어간다는 점을 강화했다.
- 근로소득 기본공제 월 116만 원, 금융재산 계산상 2,000만 원 공제, 고급자동차 4,000만 원 기준 등을 근거에 맞춰 강조했다.
- 지역별 기본재산 공제액 표를 유지·개선했다.
- 신청 가능 시점, 1961년생, 신청 장소, 준비서류, 배우자 금융정보 동의, 수급희망 이력관리 5년, 찾아뵙는 서비스 1355를 추가했다.
- FAQ 5개를 추가해 `월급이 247만 원 미만이면 무조건 수급하는가`, `부부 중 한 명만 신청해도 배우자 재산을 보는가` 같은 실질 질문에 직접 답했다.

## 검토·운영 반영

- 전체 활성 WordPress 인벤토리 39건을 새로 읽고, 대상 #101을 제외한 38건으로 중복 검사를 수행했다.
- deterministic preflight에서 구조·숫자·인용 바인딩을 통과했다.
- `manual-review --author-model "GPT-5.6 Sol"`: 독립 의미 검토 6항 모두 true, issues=[].
- 후속 `editorial_cli.py check`: READY / reasons=[].
- `scripts/prepare_post_approval.py`: READY, fresh source hash 일치, 기존 내부 링크 손실 0.
- 검토 렌더 SHA256: `61dbbfe377888580c2598bf7882dc14c7f5324228d77e1d6c9b38af44918a107`.
- `scripts/update_existing_via_ssh.py`로 #101만 기존 SHA CAS 조건으로 갱신했다.
- 전체 원본 백업: `agent-publisher/data/editorial_runs/public-edit-101-20260924T134902.json`; 백업 본문 SHA가 변경 전 SHA와 일치한다.
- 실제 저장 후 WordPress: `publish`, 수정시각 `2026-09-24 13:49:06`, 저장 SHA256 `61dbbfe377888580c2598bf7882dc14c7f5324228d77e1d6c9b38af44918a107`로 검토 렌더와 정확히 일치한다.

## 공개 브라우저 표적 QA

별도 headless Edge 세션에서 QA UTM으로 먼저 진입한 뒤 실제 공개 URL을 확인했다.

- 핵심 답변 카드 1개
- 목차 6개, 첫 앵커 클릭 정상
- 표 2개: 4행 + 3행
- FAQ 5개
- 필수 굵은 강조: 단독 247만 / 부부 395만 2,000원 / 349,700원 / 근로소득 기본공제 116만 원 / 부부 감액 20% 모두 실제 DOM `<strong>` 확인
- 360px / 390px / 1280px 문서 전체 가로 넘침 없음
- 390px, 1280px 캡처를 눈으로 확인했고 제목·금액 강조·본문 간격에 이상 없음
- QA 결과 `pass=true`

내부 bundle, 공식 source snapshot, approval package, 화면 캡처는 `tmp/post101-refresh-20260924/`에 두며 Git에 포함하지 않는다.

# #225 추석 은행 안내 — 첨부자료 직접 조사·독자 답변 완결 (2026-09-22)

## 요청과 확인된 문제

사용자는 225번 글이 공개된 금융위원회 첨부표를 독자에게 찾아보고 대조하라고 지시하는 점을 지적하고, 작성자가 직접 신뢰할 만한 자료를 조사해 실제 값을 본문에 제공하도록 수정하고 재발을 방지하도록 요청했다. 수정 전 글은 `publish`, 본문 SHA-256 `62eb3b53459c42ed07148b3d8eca33a2c67cebeb25aab6fbd9040591fe4c6826`. 공개 승인 범위는 기존 #225 갱신과 공통 작성 절차 개선이다.

## 원자료와 정확성 검증

- [금융위원회 2026-09-14 보도자료](https://fsc.go.kr/no010101/87706)와 [같은 공지의 공식 PDF 원본](https://fsc.go.kr/comm/getFile?srvcId=BBSTY1&upperNo=87706&fileTy=ATTACH&fileNo=2) 10쪽 `참고2 이동·탄력점포 운영 계획`을 직접 읽었다. 원본 PDF 크기 1,302,599바이트, SHA-256 `fd821be5aaf889aaba9a80a51a508cb670a4ad2503be7f9f55dd2f8361aad440`.
- pypdf 6.19.0의 레이아웃 추출 결과와 원본 10쪽을 2배 렌더한 화면, 정부 정책브리핑 HWPX의 표 내용을 교차 대조했다. 결과는 **12개 은행, 이동점포 11행, 탄력점포 11행, 총 22행**이다.
- 병합 셀을 단순 텍스트로 추출하면 은행명이 바로 다음 행에 잘못 귀속될 수 있어 PDF 병합 셀 테두리를 대조해 화성휴게소=신한, 인천1터미널 24시간 4개 환전소=우리, 양재 만남의광장=하나, 인천1터미널 24시간 2개 환전소=국민으로 확정했다. 전체 22행 은행명·장소·기간·시간·업무를 재확인했다.
- 주요 발견: 9월 23일 이동점포 10곳, 24일 9곳, 25~27일에는 광주은행 영광 상사화 축제 신권교환 점포 1곳(09~16시)만 운영 계획에 포함된다. 개별 탄력점포 표의 업무는 환전 위주이고 `송금`을 각 지점에서 취급한다고 표시하지 않았다.
- 신권의 권종·실시간 재고·고객당 교환 한도 및 개별 24시간 환전 카운터 이름은 원자료에 없다. 이 항목들은 임의로 채우지 않았다. 청주공항 환전소 시간은 PDF에서 행이 나뉘어 있지만 `06시~21시 50분`을 확인해 표에 완성했다. 이 표는 공식 발표 당시 **운영 계획**이며 영업 중 상태를 실시간 확인한 결과는 아니다.
- 작업용 재현 자료: `tmp/post225_work/research_official_rows_20260922.json`(행별 연속 인용), `research_official_20260922.md`, `research_banks_20260922.md`, `2026_fsc_reference2_page10.png`, `_generate_research_official_20260922.py`. `tmp`는 Git에서 제외되는 로컬 연구·검토 저장소다.

## 수정과 공통 방지 규칙

- 검토된 원고의 기존 금융 일정·FAQ와 게시물 ID를 유지하고 신권교환 11곳, 공항 환전 11곳을 각각 은행·장소·운영일·시간·업무 5열 표로 추가했다. FAQ도 실제 운영 점포의 장소·기간·시간을 직접 답하도록 교체했다. 핵심 답변에는 9월 25~27일의 영광 점포를 명시했다.
- `agents/editorial_writer.py`에 공식 PDF 바이트 식별과 레이아웃 텍스트 수집을 추가하고 `pypdf==6.19.0`을 의존성에 명시했다. `docs/EDITORIAL_SYSTEM.md`, 모델의 작성·독립 검토 지침, `editorial_policy.json`의 금지 패턴을 보강해 독자가 첨부를 찾아보도록 떠넘기는 원고를 막도록 했다.
- `agents/editorial.py`에서 해당 연도·게시물의 예외는 보도자료와 PDF 두 URL이 함께 있어야 하며, 점포 상세 표 22행이 없으면 통과하지 않도록 했다. 검토는 문단뿐 아니라 표의 각 행에도 원문 인용을 요구한다.
- 새 회귀 테스트 `test_editorial_pdf_sources.py` 2개, `test_reader_handoff.py` 2개, 해당 게시물 정책 테스트를 추가·수정했다. 공통 서식 렌더러와 다른 미커밋 변경은 손대지 않았다.

## 실제 운영 반영·검증

- 새 원고 `tmp/post225_work/post225_complete_unreviewed.json`은 `editorial_cli.py review`로 별도 내용 검토했고, 6개 검토 기준 모두 참, 이슈 0, `check` 결과 `ready`. 파일 이름은 초기 생성 이름을 유지했지만 **내부 `review` 필드가 추가된 검토 완료본**이다. 보고서는 `post225_complete_review_report.json`.
- 프로젝트 PYTHONPATH를 설정한 뒤 편집 관련 테스트 43개, 독자 전가 차단 2개, 게시물 한시성 테스트 3개 통과(총 48개). PYTHONPATH를 누락한 첫 묶음 테스트의 import 실패는 실행 환경 설정 후 해소되었으며 소스 결함이 아니었다. `git diff --check` 통과.
- 운영 서버의 가상환경에 `pypdf 6.19.0`을 설치했다. 편집 잠금·기존 파일과 커밋 기준 버전을 확인한 뒤 `agents/editorial.py`, `agents/editorial_writer.py`, `editorial_policy.json`, `docs/EDITORIAL_SYSTEM.md`만 원본 백업·SHA 검증 후 배포했다. 백업: `/home/ubuntu/agent-publisher/data/editorial_runs/post225-reader-details-20260922/backups/`. 서버 코드 구문 검사 통과. 운영 서버에는 별도의 `requirements.txt`가 없어 파일을 무작정 생성하지 않고 현재 가상환경에 의존성만 추가했다.
- `editorial_cli.py update-existing`의 공개 본문 원본 SHA 비교, 공식 출처 재조회·해시 일치, 새 검토 원고 해시·기존 제목·슬러그·게시일·대표 이미지 ID 362 보존 검사를 거쳐 #225를 갱신했다. 갱신 전 WP 전체 백업: `/home/ubuntu/agent-publisher/data/editorial_runs/public-edit-225-20260922T111440.json`. 갱신 후 본문 SHA-256 `a3b4b6f55121dbb864609f37de5196faacc9c091aabce91b69143b5524bcd50a`.
- 공개 URL: <https://lifeinfo24.org/?p=225>. HTTP 200, WordPress `publish`, 원고와 운영 화면의 표 22행 전체 셀·목차 앵커·FAQ 5개·공식 출처 2개·대표 이미지 ID 362 일치 검증 완료. 독자 자료 조회 전가 문구 제거 확인. QA 방문 UTM 태그 사용.
- Chromium 실제 렌더링: 데스크톱 1280×960 / 모바일 390×844. 소제목 굵기 700, 가로 페이지 넘침 없음. 5열 점포 표는 모바일에서 명시적 안내와 가로 스크롤로 볼 수 있다. 360px, 200% 확대, 보조기술 검사는 수행하지 않았다. 첫 브라우저 실행은 남아 있던 디버깅 포트 파일로 실패했지만 검사 스크립트의 포트 재탐색을 수정해 재실행 통과했다.

## 보존과 시의성

다른 공개 게시물과 WordPress 테마·미디어는 변경하지 않았다. 본 변경은 2026년 추석 연휴 운영 계획이며 9월 27일 이후에는 과거 일정임을 구분하거나 최신 공식 자료로 대체해야 한다. 개인별 약정과 실제 점포의 당일 운영·현금 재고는 발표자료의 확정 사실과 구분한다.

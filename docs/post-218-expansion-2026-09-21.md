# 공개 글 #218 상세 원고 준비·운영 반영 — 2026-09-21

## 요청·준비

- 사용자가 기존 공개 글 #218에 이전에 작성한 상세 안내를 적용하도록 명시적으로 요청했다. 첫 시도에서는 운영 본문이 변경되지 않았으며, 당시 재조회한 게시물은 `publish`, 수정 시각 `2026-09-21 14:37:03` KST, 원래 HTML 본문 SHA256 `65b2b8fc3437b3c21cf632c0d8fffc73dc83318d08506ca75e74c9b8ce97842d`였다. **후속 재시도로 운영 반영을 완료했다.** 아래 실제 검증 기록 참조.
- 제안 원고 `content/revision-proposals/post-218-expanded-2026-09-21.md`를 검토해 구조화 원고로 다시 작성했다. 검토된 HTML 사본 `content/revision-proposals/post-218-reviewed-2026-09-21.html`에는 7개 섹션·6개 FAQ가 있으며 내부 편집 메모가 없다. 기존 글의 제목·고유주소·공개 상태를 유지하고 본문만 갱신하는 것이 목표였다.
- 금융위원회 2026-07-07 발표·2025-06-30 FAQ·2021-11-02 간편청구 안내, 생명보험협회 조회 안내, 서민금융진흥원 온라인 신청/절차 공식 웹페이지 총 5개를 조회하고 원문 SHA256을 별도 보존했다. 이후 5개 전부 재조회 시 해시 일치. 실제 로그인, 보험 조회 결과나 은행 입금은 시험하지 않았다.
- 공통 구조화 편집 검사 `ready`, 별도 모델 의미 검토 6개 항목 모두 `true`, 문제 0건. 이 결과는 출처의 모든 상황이나 실제 신청 성공을 보증하지 않는다. 최종 검토 bundle은 Git 제외 `tmp/post218_work/post218_reviewed.json` 및 서버의 접근 제한된 `data/editorial_runs/deploy-post218-20260921/post218_reviewed.json`에 있다.

## 첫 시도에서 차단과 복구

- 금융위원회 페이지의 조회수 표시가 요청마다 변경돼 기존 `fetch_sources()`의 원문 해시 검사가 실패하는 문제를 확인하고, 본문 변경을 계속 감지하면서 독립된 조회수 줄만 제외하는 최소 수정과 테스트 2개를 작성했다. 로컬 전체 테스트 및 별도 테스트, 공백 검사는 성공했다.
- 안전한 갱신 전 서버의 원래 `agents/editorial_writer.py`를 접근 제한된 `data/editorial_runs/deploy-post218-20260921/editorial_writer.py`에 백업한 뒤 개선판을 일시 반영했다. 최초 `python3 editorial_cli.py update-existing`는 시스템 Python에 `bs4`가 없어 **쓰기 전에 실패**했다.
- 정확한 venv인 `./venv/bin/python`을 확인했으나, 재시도 명령은 도구에서 보안 상태를 판단하지 못해 실행이 차단됐다. 검사 우회나 WordPress 직접 수정은 하지 않았다.
- 서버 개선판이 백업 파일과 다른 점을 확인하고 편집 잠금이 없음을 확인한 뒤 **서버 원래 `editorial_writer.py`를 복원**, `py_compile` 성공. 읽기 전용 WordPress 재조회 결과 #218의 공개 상태·기존 수정시각 그대로. 운영 본문 갱신·캐시 변경은 수행하지 않았다.

## 후속 재시도 및 실제 운영 반영

1. 기존 서버의 `agents/editorial_writer.py`가 이전에 백업된 원본과 동일함을 확인하고, 조회수 메타데이터만 해시에서 제외하는 이미 검토·커밋된 코드로 갱신했다. 개선된 서버 코드 SHA256은 `85f5ab6c8891839c3856a588e9565a82b57a32058ffac9b2dd597b02cdc71291`이며 `py_compile`에 성공했다. 전체 출처 재검증을 제거하거나 판정값을 강제로 바꾸지 않았다.
2. 서버에서 전용 venv와 편집 잠금 해제 상태를 확인했다. 서버가 서민금융진흥원 도메인에 직접 연결할 때 시간 초과가 발생했으나, 같은 공식 URL에 대한 접속을 사용자 로컬 컴퓨터 경유 **일시적인 SSH 루프백 SOCKS 전달**로 재시도해 실제 공식 원문을 다시 조회했다. 이 연결은 해당 CLI 실행에만 적용했고 운영 서버의 영구 프록시·방화벽 설정을 변경하지 않았다.
3. 갱신 직전에 #218의 공개 상태, 수정 시각, 기존 본문 SHA256을 대조해 위 원본과 일치함을 확인했다. 기존 `editorial_cli.py update-existing`에 명시적인 `--post-id 218`, 원본 해시 및 `--confirm-update`를 전달했다. 명령이 원고 검증·별도 의미 검토·현재 공식 출처 해시 재조회·원문 충돌 검사 후 `Updated public post ID: 218`을 반환했다. 신규 글 생성이나 다른 글 수정은 하지 않았다.
4. 수정 후 WordPress 조회: ID `218`, 상태 `publish`, 기존 제목 `내보험 찾아줌 숨은 보험금 조회·청구 방법과 지급 조건`, 기존 고유주소 유지, 수정 시각 `2026-09-21 17:09:32` KST. 새 본문 SHA256은 `f687eaf0d7aad38d029882c951a8f1954f3b0c9da328bf06bb1126f1dbabb6bd`이고, Git에 저장된 검토 HTML과 **문자 단위 완전 일치**했다. 7개 단계·6개 FAQ·공식 근거 링크가 있으며 내부 편집 메모는 노출되지 않는다.
5. 공개 URL `https://lifeinfo24.org/?p=218` 직접 HTTPS 조회는 `200`이며 기존 canonical 주소로 이동했다. 공개 렌더링에서 원고의 7개 제목·6개 FAQ·첫 문단·2개 공식 행동 링크가 모두 일치했다. 이는 실제 브라우저에서 로그인 후 보험금 조회·신청·지급을 완료한 테스트가 아니다. HTTP 조회에서 새 내용이 보이므로 별도 캐시 강제 삭제는 하지 않았다.
6. 전용 수정 기능이 저장한 기존 게시물 전체 롤백 사본: 서버 `/home/ubuntu/agent-publisher/data/editorial_runs/public-edit-218-20260921T170930.json`. 파일 권한 `0600`으로 제한했고, 원본 ID `218`·공개 상태·기존 본문 SHA256 일치를 확인했다. 로컬 Git 제외 경로 `tmp/post218_work/public-edit-218-20260921T170930.json`에도 복사했다. 복구는 필요한 경우 이 백업과 게시물 수정 이력을 대조한 후 #218에만 수행한다.

**검증 범위:** 별도 모델의 6개 의미검토 항목 통과·이슈 0, 구조화 원고 검사 `ready`, 이 작업 전 로컬 전체 unittest 164개 통과, 서버 전용 CLI 성공 및 사후 WordPress/공개 HTTPS 검증. 실제 사용자 인증·개별 보험금 조회·입금이나 모든 외부 캐시의 즉각적인 갱신까지 검증한 것은 아니다. 다른 작업자의 사전 미커밋 코드 4개와 미추적 감사 문서는 보존했다.

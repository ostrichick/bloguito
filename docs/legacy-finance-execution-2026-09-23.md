# 구형 공개 금융 3건 재검증·보류 후보 및 최신형 2건 좁은 감사 — 2026-09-23

**기준: 2026-09-23 09:09~09:13 KST. 최종 운영 적용 판정: #125 / #139 / #220 모두 HOLD, #127 / #304 새 수정안 승인 없음.** 이 문서는 오늘의 독립 실측과 차단 이유만 기록한다. 기존 `docs/legacy-full-bundles-finance-2026-09-22.md`, `docs/legacy-modernization-progress-2026-09-23.md`, `docs/legacy-health-refund-readiness-2026-09-23.md`를 재작성하거나 어제 승인 패키지를 최신 것으로 취급하지 않는다. **이 담당자의 실제 수정은 이 문서와 Git 제외 `tmp/legacy_audit_20260923/finance2/`뿐**이다. WordPress/운영 서버·공개 글·공통 소스 코드·정책·다른 담당자의 작업물은 읽기 전용이며, 신규 독립 모델 리뷰·정규 CLI check·운영 승인 패키지·Git stage/commit/push·배포는 수행하지 않았다.

## 1. 판정 정책과 완전 재고

- `AGENTS.md:3-13`, `docs/EDITORIAL_SYSTEM.md:11-27,29-35,41-50,58,72-103,111-115`, `agent-publisher/editorial_policy.json:3-20`: **실제 공식 원문**, 연도·코호트·제외조건·독자 질문, 문단/표 행별 원문 연속 인용, 현재 전체 게시물 재고, 출처와 독립 검토 각각 24시간, 날짜형 유효기간 최소 30일, 실제 목적지 행동 링크, 독립 여섯 항목 전부 참, 현재 WP 저장본문 SHA/백업 및 **게시물별 최종 승인**을 별개로 요구한다. 유효기간 끝과 개별 신청 마감을 혼용하거나 출처 SHA 불일치·중복을 비활성화할 수 없다. 전일의 `review_passed=true`와 `prepared_not_authorized`는 새 후보의 승인 근거가 아니다.
- 재고는 오늘 총괄 담당이 읽기 전용으로 확인한 **공개 29 + 초안 10 = 활성 39건**, 별도 휴지통 6건의 현재 기록을 재사용했다. `tmp/legacy_audit_20260923/worker-flu/post-63-inventory-live.PRIVATE.json`(자기 #63을 제외한 38건)에 같은 날 완전한 공개 REST `worker-inventory/public-rest-snapshot.json`의 #63 **한 건만** 더해 39건으로 검증하고, 각각 자기 #125/#139/#220을 제외한 **38건**을 메모리에서 검사했다. 비공개 초안 제목·본문·개인정보를 보고서에 공개하지 않았고, 휴지통 글을 정상 공개/초안으로 취급하지 않았다. 이 결합은 오늘의 읽기 전용 WP 39건에 기초하지만 각 대상의 **09:13 이후 저장 `post_content`/CAS SHA를 다시 조회한 증거는 아니다.**
- 실제 `agents.search_intent.duplicate_posts()` 결과(자기 ID 제외): #125 → 공개 **#139**, 초안 **#135**; #139 → 공개 **#125·#220·#243**, 초안 **#135**; #220 → **0건**. #125와 #139는 제목·주제 겹침 및 같은 NTS 출처의 기존 본문 링크, #139와 #220/#243은 공단/스마트초이스의 **본문 또는 행동 위치**에서 같은 공식 URL이 발견됐다. 현재 코드는 이미 단순 최신형 출처 *각주* 공유를 무조건 중복으로 보지 않으므로 `duplicate_topic`을 일괄 오탐으로 폐기하지 않는다. 초안 #135는 별도 승인 권한자가 주제 중복·보존 필요성을 검토해야 하며 이 담당자가 삭제·병합·승인할 근거가 없다.

## 2. 공식 원문 실측: 정책 본문과 동적 메타를 구별

`finance2/probe_readonly.py`로 구번들의 **각각 정확한 공식 URL**을 순서대로 2회 GET, 현행 `agents.editorial_writer.fetch_sources()`만 사용하여 HTML→source.text/SHA 계산. 각 URL의 원문·fetched_at·SHA 및 원문 대비 diff와 모든 기존 evidence quote 문자열 일치 검사는 `finance2/post-{ID}-{source-probe,fresh-sources}.json`에 있다. **전 대상 5건의 전체 소스 취득 성공; 모든 재수집된 source에서 구번들 인용 원문 완전 일치, 누락 0.** 인용 문자열 존재는 각 주장 의미/최신성에 대한 독립 검토를 뜻하지 않는다.

| 게시물 | 오늘 동일 URL 연속 재수집 SHA와 전일 대비 차이 | 실제 결론 |
|---|---|---|
| #125 국세청 세금포인트 | `nts.go.kr/...nttSn=1355091` 두 SHA `51145d16…` → `1fbd504f…` **서로 다름**; 어제 `a32a7b35…`와 7줄 diff는 조회수 **5746→6709**, 본문·제목·작성일·사용처/코호트 문장 변동은 diff에 없음. | 동적 카운터가 원문 체크마다 새 SHA를 만드는 원인이지만 **무시 승인 금지**. |
| #139 네 기관 미환급금 | Korea.kr 원클릭 2025-03-31 정확한 브리핑 추출 SHA `fa2d2a175ca5…` **2회 같음**, 어제 거대한 전 페이지 추출과 달라 diff 195줄(본문 밖 뉴스 레일·공통 주변 요소 제거); 원고에 연결된 기존 인용은 모두 일치. NHIS/스마트초이스/대전시 3건 SHA 어제와 동일. NTS 한 URL 두 SHA `6f263266…` → `31265bd3…` **다름**; 어제 `f35eca2d…` 대비 7줄은 조회수 **5744→6711**뿐. | Korea.kr 기존 좁은 추출 분기는 정상; **NTS 카운터만 여전히 불안정**, 공식 의미 검토와 중복은 별개. |
| #220 본인부담상한제 | MOHW 보도자료 `list_no=1491727` SHA `9fc075460706…` **2회 같으나** 원래 `c41c1323…`와 12줄 diff는 첨부 HWPX 다운로드 `871→880`/미리보기 `352→355`, PDF 다운로드 `807→817`/미리보기 `297→298`뿐. 파일명/크기 `185.21KB`·`659.25KB`, 본문 비교 불변. NHIS `676c93abc84c…` 동일. 두 원문 기간 라벨 추출 **각 0개**. | 메타 정규화 후보는 가능해도 실제 신청 기간·접수 상태 근거는 새로 얻지 못함. |
| #127 (이미 최신형) | 9/22 최종 배포 **정확한** `post-127/resume-reviewed-20260922-161139-955821/v2-20260922-161958-994675/bundle.json`의 Korea.kr 2, NTS 1, 손택스 조회 1, 법령 1 **총 5개 출처 모두 2회 동일 SHA, 어제 최종 bundle SHA와 일치**, 인용 누락 0. | 9/22 적용을 **다시 실행하지 않는다**. 링크 기능의 동적 브라우저 차단은 별도 문제. |
| #304 (이미 최신형) | E-순환거버넌스의 새 규격 이용안내, 옛 품목별 안내, 예약 시작화면 **3개 모두 2회 동일 SHA, 전일과 완전 일치**. | 사실충돌이 해결되지 않은 상태로 보전됨. |

## 3. 전편 수정 후보 세 건: 실제 생성·검증 범위

`finance2/build_blocked_candidates.py`는 전날 각각의 **전체** `bundle.json`을 출발점으로 오늘 공식 원문 `sources` 전체 교체(`text` 자체 SHA-256 검증), 원고 문단의 제한적인 사실 표현 수정, 기존 `review`/`reviewed_model` **완전 제거**, 인용 연속 문자열 전체 검사, 오늘 본인 제외 38건 inventory의 `duplicate_posts()`, `validate_bundle(require_review=False)`, 정규 렌더러 **완전 HTML** 생성을 수행한다. 결과는 `finance2/post-{125,139,220}-candidate.BLOCKED.json`, `-candidate.html`, `-candidate-preflight.json`, `-candidate-status.json`. 파일명과 상태에 `BLOCKED`를 강제해 과거 `ready`로 오인시키지 않았다. 모든 후보 sources SHA↔text 검증 통과·출처와 연결된 **모든 인용이 오늘 원문에 존재**, 원고의 lead/섹션/표/FAQ를 합친 전체 구조 보존. **이 단계의 사전검사 하나라도 실패하므로 모델 리뷰, CLI check, approval prepare는 규약대로 실행하지 않았다.** 재현 스크립트는 본인 finance2만 쓰며 운영 사이트·WP 읽기/쓰기 도구 자체를 호출하지 않는다.

### #125 — 구체적 표현·인용 7블록 보정, 나머지 세 차단 유지

- 원본 2026-09-22 사전검사의 엄격 수치 인용 7개 문제를 원문 표현에 맞춰 **오늘 재수집 소스에 연결하여 전부 해소**했다. `2025년에/2025년 부여분`을 같은 공식 자료의 `’25년에/’25년`으로 한정해 표현하며 `2014년`은 `’14년`, `1,000원 할인`은 원문 그대로 `1천원`, 발표일은 `2026.09.17.`, 사용처 기준은 `’26.9.`로 바꾸었다. `5년`을 주장한 lead·제도 설명·FAQ에는 실제 연속 원문 `소멸\n기한\n5년`, 연 한도에는 `※ ’25년에 부여하는 포인트부터 한도 적용` 근거를 추가했다. 이 조치는 **개인 5년 소멸은 ’25년에 부여하는 포인트부터, 법인은 ’14년부터 / 연간 1,000포인트 한도는 ’25년 부여분부터**의 다른 조건을 섞지 않는다. 넷마블게임박물관 10/1 쿠폰 발급 시작, 아쿠아리움 9/11 시작을 서로 바꾸거나 현재 모든 쿠폰 사용 가능으로 확대하지 않았다.
- 오늘 **수치 직접 인용 검사 `number_without_evidence` 0건**, 본문 전체 quote 0건 누락. 그러나 `critical_facts.py:54-61`의 출처 guard는 4자리 `2025`를 요구하고 국세청 공문은 **`’25년에 부여하는`**으로 표기한다. 같은 가드는 기사 body에 `5년`이 있으면 4자리 `2025` + `부여`를 요구한다. 숫자 체크를 지키기 위해 공식 축약 형태로 수정한 결과 두 이유 `tax_points_2026_authoritative_version_missing`, `tax_points_2026_expiry_cohort_missing`이 남는다. 전체 재고의 `duplicate_topic`(#139, 초안 #135)과 NTS 매회 SHA 변동도 독립적 차단. 원문에 없는 가짜 `2025`를 quote에 추가하지 않음. **상태 `needs_review`: 이 세 결정론적 이유가 모두 해소되기 전 독립 리뷰·CLI·approval 불가.**

### #139 — 네 기관별 길찾기만 남기는 전체 후보; 중복만 코드 차단, 운영 승인 아님

- 제안 제목을 `국세·지방세·건강보험·통신 미환급금 4종: 기관별 조회와 세금포인트 차이`로 좁히고 독자 질문도 **네 서비스 각각의 서로 다른 공적 경로/세금포인트는 현금이 아니라 별도 혜택**으로 특정. 빠르게 시효가 변하는 9월 박물관 신규 쿠폰 홍보 문단 하나만 빼고 4행 기관 비교표·국세 확정 미수령↔미신고 원클릭 차이·지방세 위택스·NHIS·스마트초이스·세금포인트 별도 혜택 설명·FAQ까지 **전편 보존**. 특정 당해 세금포인트 부여 규칙을 여기서 재진술하거나 `2026`을 제목/주제 키에 부가해 #125 최신 코호트 검사를 우회하지 않았으며, 이 글의 주제가 실제로 연도와 무관한 네 기관 조회 안내인 점만 반영했다. 이 출처들은 **#125의 별도 전문 글을 자동 폐기해도 된다는 근거는 아님.**
- 오늘 실측 새 Korea.kr 정밀 추출·원문 전부 수집/인용검사 통과, 날짜형 거짓 `useful_until` 없음, `number_without_evidence`와 구 세금포인트 코호트 guard 문제는 전편 후보에서 발생하지 않음. **남은 결정론적 차단은 `duplicate_topic` 1종**(공개 #125/#220/#243, 초안 #135); NTS 원문 SHA는 여전히 회전. 공개 다른 글의 근거 URL이 내용 본문/동작 링크로 쓰인 경우도 있어 단지 URL 문자열만 바꿔 허위 독립 주제를 만들지 않는다. 4종 범주형 허브로 분리·각 전문 글 내부링크/중복 문단 범위와 보존할 #135을 승인 권한자가 결정하고 **원문 출처 및 인용·정규 검증을 다시 수행**해야 한다. 오늘 `preflight=needs_review`, 모델 미호출.

### #220 — 전체 후보를 날짜 명확화했으나 신청기간 근거는 없음

- 과거 전체 원고의 다섯 섹션·두 FAQ를 유지하고 `보건복지부는 올해 10.8. 시행 예정`을 **`보건복지부는 발표 당시 10.8. 시행 예정이라고 밝힌`**으로 수정해 2026-08-30 발표의 미래형이라는 사실을 현재 시행 완료로 오해하지 않도록 했다. `올해 낸 병원비` FAQ 질문을 **`2025년에 낸 병원비`**로 바꿔 2026 지급·2025 진료연도 혼동을 줄였다. 실제 2025년 진료분 226만 2,605명/3조 760억·개인별 2025 상한 89만~1,074만·사전등록 계좌 9/2 자동지급과 개별 신청·제외 항목은 당일 본문 인용 그대로 보존. 둘 다 전체 source-bound이고 자체 숫자/원문 인용 사전검사 추가 실패 없음.
- 오늘 독립적인 `duplicate_topic` 0, 다른 사전검사 실패는 **`availability_not_verified` + `temporal_source_not_bound`**. 원문 두 URL 모두 `extract_evidence()`에 통과하는 신청기간/상태 라벨 0건, `2026-12-31`은 임의 `brief.useful_until`이지 공단이 제시한 개인 신청 마감 아님. `8/31`은 통지 시작, `9/2`는 사전등록 계좌 지급 시작, `10/8`은 공제 규정 시행 예정일이므로 종료일로 끼워 넣지 않았다. 현행 `welfare` evergreen 전환은 `dated_category_cannot_bypass_time_check`, `legacy_reference_period` 허용 ID는 55/79/101/103뿐, #225 기간 예외는 이 글에 사용 불가. 자세한 증거별 안전 경로는 **본인 9/23 선행 보고서** `docs/legacy-health-refund-readiness-2026-09-23.md`의 §2–4와 동일하며 다시 조사한 부분은 오늘 변동값/후보 결과뿐이다.

## 4. 최신형 #127·#304, 해결 근거 없으므로 수정 후보를 만들지 않음

**#127 (어제 운영 적용 완료):** 실제 운영 저장 본문 SHA는 어제 최종 기록 `90da96d221408a5eb726014ed1dc5af7345160e2a6a91ad6ec6f380def761f66`, 현재 9/23 새 저장 SHA는 이번 담당 미조회. 현행 원고의 손택스 출처 `https://mob.tbht.hometax.go.kr/jsonAction.do?actionId=UTBRDAAA03F001`는 어제 Edge JS 탐색에서 홈택스 메인으로 이동해 **직접 조회 폼 도착 미검증**(`docs/resume-layout-current-census-2026-09-22.md:77`). 오늘 `finance2/narrow_audits_readonly.py`가 비인증 public GET로 요청 기본 UA와 Edge 모사 UA 각각 HTTP **200, 최종 URL 변경 없음**, 정적 HTML에서 `국세환급금찾기`, 주민/사업자번호·성명 입력 레이블을 확인했고, 웹의 정적 페이지 판독도 동일하다. 이것은 **정적 HTTP와 브라우저 내 JS 내비게이션 결과가 달랐다는 재현 범위의 차이**이지 어제 버그가 해결됐다는 증명이 아니다. 실제 브라우저에서 링크 클릭→대기 후 최종 URL/화면·정상 폼·실제 기능(개인정보 미입력)을 새로 검증하고, **직접 조회 링크로 유지할지 공식 홈택스 메인→메뉴 경로 안내로 교체할지** 결정해야 한다. 오늘은 링크 교체·행동 CTA 추가·신규 원고 후보 0건. #127의 9/22 검토 완료 구조·본문은 그대로 보존한다.

**#304 (9/21부터 최신형):** 9/23 E-순환거버넌스 최신 `https://www.15990903.or.kr/portal/cnts/userGuide.do`는 기타 전자제품의 가로+세로+높이 **150cm 이상 또는 무게 12kg 이상**을 중형 이상 단일 품목(1개), 50~150cm 미만/1~12kg 미만 중소형은 다량(5개 이상)으로 분류한다. 반면 별도 공식 `https://www.15990903.or.kr/portal/cnts/selectContents.do?cntnts_id=A1000005`에는 **전자레인지**가 규격 표시 없는 `단일수거 가능 품목`으로 여전히 명시된다. 두 공식 원문이 **당일 2회씩 어제 SHA와 동일**하고, 공개 예약 시작화면은 개인정보 동의 이후에야 배출품목 입력 단계가 이어져 전자레인지 1대의 실제 선택·예약 적합성을 확인할 수 없다. 운영기관 공개 FAQ `https://www.15990903.or.kr/portal/faq/selectPageListFaq.do` 공개 GET에도 전자레인지 우선분류 정정이 발견되지 않았다. 두 주장을 합쳐 `모든 전자레인지 1개 확정`이나 역으로 `모든 전자레인지는 5개 필수`를 쓰지 않는다. 기존 안전한 경고 단락은 이미 게시 중이고 9/22 `post-304` 새 전체 리뷰·CLI ready였어도 **수동 충돌 HOLD**가 우선. 기관의 서면 기준·공식 수정 공지/표 또는 비인증 상태에서 검증 가능한 정당한 품목 분류 증거가 확보된 후에만 별도 좁은 원고·전체 재리뷰 가능. 새 후보·라이브 변경 **0건**.

`finance2/127-304-public-get-audit.json`에는 두 User-Agent별 HTTP 상태·최종 URL·폼 텍스트 존재·공식 페이지 키워드만 담았고 이용자의 주민번호, 계좌, 이름 등 어떤 양식 데이터도 제출하지 않았다. 정적 GET을 실제 인증·신청·예약 성공으로 쓰지 않는다.

## 5. 총괄 소유자에게 제안하는 최소 코드 변경 및 최종 게이트

**이 담당자는 공유 코드 변경 금지에 따라 다음을 적용하지 않았다.** 변경은 코드 소유자가 판단·테스트·커밋할 최소 범위이다.

1. **NTS bare host 정확한 메타 정규화** — `agent-publisher/agents/editorial_writer.py:177-183`의 hostname 허용 집합에 `nts.go.kr`를 더하되 경로 `/nts/na/ntt/selectNttInfo.do`, DOM `.bbs_ViewA .bbsV_data > li`의 `조회수` + 정수 패턴이 실제 정확히 맞는 **메타 항목만** 삭제한다. 기사 본문·표의 1,000/5년/’25·파일명/날짜는 SHA에서 유지하고 구조 변경 시 fail closed. 카운터 값만 바뀌는 정밀 회귀, 기사 수치/첨부 변경 시 해시 변화, 범위 밖 게시물 미정규화 테스트 필요. 오늘 두 연속 수집에서 실제 SHA가 각각 달랐으므로 이 수정 없는 새 승인 사전검사는 즉시 불안정하다.
2. **MOHW 이 보도자료만 첨부 카운터 정규화** — 같은 파일 `:199-225`의 기초연금 정확 ID `1488478` 분기를 무작정 전체 `board.es`로 넓히지 말고, **`list_no=1491727`/bid=0027/act=view/mid 동일**을 개별 허용하며 `div.file > ul.list > li`의 첨부명·크기 보존/다운로드·미리보기 카운터만 제외한다. 파일 2개·정확 HTML span 구조 검증, 미확인 형식·실제 본문/파일크기 변경 fail closed, 회귀 테스트 필수. **이것은 #220 기간 증거 문제를 절대 해결하지 않는다.**
3. **공식 축약 연도 동등성의 아주 좁은 수치/정책 검사** — `agents/critical_facts.py:54-61`은 공식 국세청 `’25년에 부여`, `’14년부터` 표현을 확인한 뜻(연간 한도 2025 부여분, 개인 5년 기산 2025, 법인 2014)으로만 인식하도록 변경하거나 아예 `2025년`이 완전 표기된 추가 최신 **공식 원문**을 확보. 기사·quote 양쪽의 숫자 매칭도 `2025`↔`’25`를 세금포인트 **부여 코호트**에 한정 검증; 표기만 맞추려고 공식 quote를 조작하거나 국세청 최신 연도 하드게이트를 전역 해제하면 안 된다. `1,000포인트`/`5년`과 적용 대상·연도는 계속 필수, 엉뚱한 ’25 시점 기사·반대 코호트는 통과하지 못할 테스트가 필요하다.
4. **중복은 코드에서 일괄 제거 금지** — 위 실제 125/139 겹침과 비공개 #135 처리 방향을 담당자가 글별 결정하고, 별도 목적지/실제 독자 질문으로 **진짜 독립**인 경우에만 전체 재고·본문/행동 링크를 검사한다. 명백한 정상 출처 footer 공유 오탐을 확인할 경우에도 정확 source footer DOM만 수정하여 주소·주제·CTA의 실중복 검사를 유지한다.
5. **#220** 공식 공단/복지부가 적용 대상 2025 진료분의 실제 신청기간·상태와 종료일을 공개하고 **현행 30일 이상 남는 것**을 직접 입증한 경우에만 날짜형 상태 재평가. 종료일이 없는 상시 청구 제도에 한정한 신규 정책 경로는 편집 정책 책임자의 명시적 범위/승인과 정확한 시행일·재검토 주기·개인별 기한 처리·관련 테스트 없이는 도입 금지. 임의 `useful_until=12/31`/기초연금 예외 복제/복지 evergreen 재라벨 금지.

**허용되는 후속 순서:** 각 위험 근거를 해결한 뒤 당일 최신 공식 원문 정밀 재수집·전체 39건 이상 실제 WP 상태 재고/자기 제외·서면으로 구체적인 중복 처리 → `validate_bundle(require_review=False)` *진짜* `ready` → 새로운 별도 모델 의미 검토 6/6, 모든 수치/인용/행·FAQ 재검사 → 정규 `editorial_cli.py check` → 원본 WP 저장/원본 전체 백업·현재 CAS/제목/상태·모든 공식 URL SHA 재검사 및 읽기 전용 approval package → 독자가 읽을 전체 비교와 **게시물별 사용자 승인** → 별도 운영자가 정규 updater로 작업 후 REST/실브라우저 확인. 이 담당자는 차단 단계에서 멈춰 안전 경계를 준수했다.

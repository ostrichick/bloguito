# 공개 글 현대화 1차 실행 기록 — 2026-09-22

## 범위·사전 상태

- 요청: 기존 공개 글을 현재의 직접 답변·공식 출처·상황별 조건·반응형 서식 기준으로 순차 개선하고, 이후 지침 발전에도 재검토 가능한 체계 마련.
- 작업 기준 Git HEAD: `93ca6d2`. 다른 작업자의 미커밋 `curator.py`, `publisher.py`, `test_article_layout_contract.py`, `test_editorial_system.py`, `test_ticket_validation.py`, `docs/EDITORIAL_SYSTEM.md`, `docs/INDEX.md`, `wordpress/mu-plugins/bloguito-legacy-callout-spacing.php`와 다른 미추적 문서를 보존했다. 일부 파일은 실행 중 다른 작업에서 변경 중이므로 본 작업의 수정 파일과 혼동하지 않는다.
- 현재 공개 WordPress REST `per_page=100`에서 `X-WP-Total=30`, 총 1페이지, 실제 공개 글 30개 중복 없이 확인. 사전 29편이라는 날짜별 조사 수치를 현재로 재사용하지 않았다.
- 공개 REST 스냅샷: Git 제외 `tmp/legacy_audit_20260922/public-rest-snapshot.json`; 항목별 구조 장부: `tmp/legacy_audit_20260922/triage.json`; 사람이 읽는 전수 보고서: `docs/legacy-post-modernization-audit-2026-09-22.md`.
- `content.rendered` SHA256은 WordPress 렌더링 HTML의 관측 해시다. `editorial_cli.py update-existing`의 원본 `post_content` 해시 대신 사용할 수 없다.

## 1차 전수 결과: 자동 구조 검사와 실제 의미 검토 구분

| 범주 | 결과 | 해석 |
| --- | ---: | --- |
| 공개 글 | 30편 | REST 1페이지의 헤더/실제 결과 일치 |
| 최신 `.bloguito-article` | 5편 | #218·#225·#243·#304·#349; 자동 탐지상 각 자체 목차 1개 |
| 구형 레이아웃 | 25편 | 신규 구조 적용을 위한 글별 원문·정보 재검토 필요 |
| 목차 없는 후보 | 25편 | 공개 REST 본문 기준. 테마·플러그인 최종 출력은 실페이지 재확인 필요 |
| 박스 직계 문단 여백 검토 후보 | 14편 | 현 스크립트의 보수적 구조 휴리스틱; 실제 여백 문제 수와 다름 |
| 독자에게 확인을 떠넘기는 패턴 후보 | 1편 | #145 문구 1건, 문맥과 공식 원문 확인 전 오류 확정 금지 |
| 알려진 좁은 사실 위험 패턴 | 0편 | `audit_published_facts.py --strict`의 탐지 0, 전체 사실 검증 완료 의미 아님 |

별도 진행된 `docs/callout-spacing-audit-2026-09-22.md`에는 구형 초록색 요약 상자 **24편**에서 잘못된 닫는 `</p>`가 확인되었다. 이 검사는 위의 **직계 문단 여백 후보 14편과 측정 항목이 다르다**. 현재 별도 담당자가 준비한 MU 플러그인·지침·테스트 변경은 다른 작업자의 미커밋 상태이므로 여기서 재적용·덮어쓰기·서버 설치하지 않았다.

실제 공개 URL 30편 모두 HTTP 200과 `.entry-content`를 확인했으며, 그 페이지의 목차·표 개수는 REST 결과와 일치했다. 최신형 5편의 목차 앵커 누락은 관찰되지 않았다. 구형 #85에는 `caption`·가로 스크롤 래퍼·키보드 포커스가 없는 표 2개가 있어 표의 내용·표제·행열 관계를 다시 검토해야 한다. 직접 브라우저 화면의 픽셀 겹침·모바일 360/390px·200% 확대는 별도 시각 검증 대상으로 남긴다.

추가로 기존 13편(#217·#163·#145·#144·#140·#137·#113·#99·#127·#119·#121·#105·#70)은 FAQ 소제목과 수동 질문 두 개씩을 갖지만 새 `.bloguito-faq` 구조가 없다. 구형 24편은 적어도 일부 `h2`에 `id`가 있지만 #85의 `h2` 6개는 모두 `id`가 없다. **25편의 목차 부재를 일괄 오류로 확정하지 않고 실제 글의 분량·질문과 연결성을 판단**하며, 목차가 필요한 글에만 하나씩 생성한다. 플러그인을 전면 재활성화하면 최신형 5편에서 중복 목차를 다시 만들 수 있으므로 적용 대상 한정 또는 신규 렌더러 이관을 검토한다. #85의 표 접근성은 글별 검토로 보완한다. 현재 실제 공개 #99 HTML에는 준비된 MU CSS 스타일 ID가 없어 운영 배포 전 상태다.

자동 독자 전가 후보 #145는 추석 고속도로 통행료 면제의 공식 기간을 본문에 먼저 밝히면서 “운전 전 도로의 적용 여부와 실제 교통 흐름을 공식 안내에서 확인하세요”라고 쓴 경우다. 노선별 조건과 실제 교통상황은 별도 실시간·도로별 자료가 필요할 수 있으므로 **문구 패턴 발견만으로 독자 전가 위반 확정하지 않는다**. 이 글의 노선별 공적 자료 수집 가능 범위를 추후 재조사한다. 반면 아래 #137/#55의 누락 정보는 공개된 공식 자료에서 명시적으로 확인했다.

## 근거로 확인한 정보 보강 후보

| 순서 | 글 ID | 현재 문제 | 공식 근거·조치 |
| --- | ---: | --- | --- |
| 우선 | [#137](https://lifeinfo24.org/?p=137) | 2026년 9월 상반기 근로장려금 신청을 놓친 독자에게 다음 신청기간을 국세청에서 확인하라는 문장만 제공 | 국세청 2026-09-01 안내는 2027년 3월 하반기, 5월 정기, 11월 말 기한 후 신청의 구체적 기간·대상·산정액 적용 범위를 제시. 이를 본문에 직접 제공하는 시범 원고 준비. [국세청 보도자료](https://kids.nts.go.kr/nts/na/ntt/selectNttInfo.do?mi=2201&nttSn=1354576) |
| 우선 | [#55](https://lifeinfo24.org/?p=55) | 준비 서류와 대리 신청 가능 여부를 접수기관 확인으로 넘김 | 정부24 기초연금 민원에는 신분증·통장·소득재산 신고서·금융정보 동의서·대리 위임장과 조건부 임대차계약서 등이 공개돼 있음. 2026년 현재 운영 안내와 교차 확인한 후 직접 안내. [정부24 민원](https://www.gov.kr/mw/AA020InfoCappView.do?CappBizCD=13520000048) |
| 보강 | [#79](https://lifeinfo24.org/?p=79) | 복지로 홈페이지로만 이동시키며 직접 모의계산 경로와 실제 메뉴 설명 부족 | [복지로 모의계산 메뉴](https://www.bokjiro.go.kr/ssis-tbu/twatbz/mkclAsis/mkclPage.do)와 세부 화면을 공개 브라우저에서 검증한 뒤 단계 추가. 실제 계산 결과·로그인 후 화면 미검증. |
| 보강 | [#103](https://lifeinfo24.org/?p=103) | '목포 접종 병원 찾기'라는 제목에 비해 병원 이름·전화·위치가 없어 독자가 전부 검색해야 함 | [질병관리청 지정의료기관 찾기](https://nip.kdca.go.kr/irhp/mngm/goVcntMngm.do?menuCd=333)에서 목포시 검색 결과를 실제 추출한 뒤 범위·확인 시점을 붙여 제공. 실제 목록/백신 재고는 아직 검증하지 않음. |

이 후보는 공개 WordPress 본문과 확인 가능한 공식 페이지를 대조한 **편집 완결성 문제**다. 해당 글의 다른 내용 전체가 사실 오류라는 주장이나 글별 원문 검토 완료 판정은 아니다. 특히 #225는 현재 운영 본문에 이동점포·탄력점포 각 11곳의 은행·장소·날짜·시간·업무 표가 있어 과거의 '첨부자료에서 찾기' 문제를 현재 결함으로 재등록하지 않는다. 개별 표 행의 공식 PDF 재검증은 별도 작업이다.

## 이번 작업에서 신규 작성한 항목과 검증

- `scripts/audit_legacy_posts.py`: 모든 공개 REST 페이지를 조회해 헤더의 총수와 중복·상태를 검증한 뒤, 읽기 전용 본문 스냅샷·개별 구조 진단 JSON·Markdown 보고서를 출력. 기존 확인된 사실 위험 탐지 함수를 재사용하며 사실·화면·링크 동작 검토를 각각 `unverified`로 명시.
- 실행 예시(프로젝트 루트, Windows): `./agent-publisher/.venv/Scripts/python.exe scripts/audit_legacy_posts.py --snapshot-dir tmp/legacy_audit_20260922 --report docs/legacy-post-modernization-audit-2026-09-22.md`. 이 명령은 공개 REST를 읽고 **로컬 파일만 작성**하며 WordPress의 게시물·설정을 바꾸지 않는다. 실행 전에 현재 날짜에 맞는 별도 작업 폴더를 지정하고, 생성한 보고서는 작업별 기록으로 남긴다.
- `agent-publisher/tests/test_legacy_post_audit.py`: 정상 페이지 전수 수집, 총수 불일치·중복·비공개 글 거부, 최신 구조의 구형 오분류 방지 및 깨진 목차 앵커 탐지 등 타깃 테스트 **3개 통과**.
- `scripts/audit_published_facts.py --strict`는 2026-09-22 실행 기준 알려진 제한적 패턴 0건. 이 결과로 공식 정보 전체의 진위를 인증하지 않았다.
- 시범 글의 국세청 공식 기사 실제 HTML에서 `<div class="bbs_ViewA"><ul class="bbsV_data"><li><strong>조회수</strong>65289</li>` 같은 조회수 항목을 확인했다. 기존 출처 수집기는 `조회수: 숫자` **한 줄**만 제외하므로 실제 페이지의 줄 분리된 숫자가 정책 변경 없이 원문 SHA256을 바꿀 수 있었다. `agent-publisher/agents/editorial_writer.py`에 **국세청 보도자료의 검증된 메타데이터 목록에서 조회수 항목만 제외**하는 선택기를 추가하고 기사 본문은 그대로 보존했다. `agent-publisher/tests/test_editorial_source_provenance.py`에 조회수 변경 시 동일 SHA·실제 신청 날짜 변경 시 다른 SHA 회귀검사를 추가했으며 해당 출처 검사 **3개 통과**. 초기에 새 테스트의 가짜 본문이 최소 원문 길이(80자)에 못 미쳐 1개 실패했으나 테스트 fixture를 실제 길이에 맞게 보완해 최종 재검사 3개 모두 통과. 실제 국세청 페이지의 반복 조회 해시 확인은 시범 bundle 작성 단계에 기록한다.
- 실제 `kids.nts.go.kr`의 동일 URL을 수정된 수집기로 연속 2회 읽은 결과 원문 SHA256이 일치하고 `조회수` 메타데이터는 출처 본문에서 제거됐으며, 추출 본문은 7,872자였다. 이 확인은 해당 URL·조회 시각의 일치만 증명하며 모든 국세청 페이지나 향후 DOM 개정까지 보장하지 않는다.
- 연관 영향 범위의 추가 회귀검사 `test_koreakr_source.py` 4개, `test_editorial_pdf_sources.py` 2개, `test_editorial_updater.py` 6개 모두 통과했다. 기존 국정브리핑 기사 선택, PDF 읽기, 원문 충돌 시 공개 글 수정 거절 등 인접 경로의 확인된 동작을 유지했다. 현재까지 본 작업 관련 타깃 검사 합계 **18개 통과**(신규 전수 감사 3 + 출처 메타데이터 3 + 기사 4 + PDF 2 + 기존 공개 글 수정 6). 별도 작업 중인 프로젝트 전체 테스트 통과를 의미하지 않는다.
- 이번 작업 자체의 WordPress 게시글·운영 서버·테마/플러그인·발행 상태는 변경하지 않았다. 작업 결과는 코드·문서 및 Git 제외 로컬 스냅샷으로 준비했고, 커밋·푸시 여부는 해당 Git 이력에서 별도로 확인한다. 운영 서버 배포·게시물 갱신은 수행하지 않았다.

## 다음 실행 게이트

1. #137 시범 글의 공식 최신 자료에서 신청 기간·대상·실제 메뉴·예외를 재검증하고 기존 정확한 정보를 누락하지 않은 새 구조화 bundle 작성.
2. 새 원고를 공통 `editorial_cli.py review/check`로 독립 검토하고, 원본과 수정본 비교 및 모바일 미리보기 생성.
3. 공개 글을 변경할 때 현재 WordPress 원문 SHA256을 **별도** 조회, 원본 백업·글별 사용자 승인·`update-existing`·저장 후 일치 검사를 수행.
4. 다른 담당자가 작업 중인 초록색 요약 박스 수정은 실제 운영 설치/브라우저 QA가 끝났는지 별도 기록을 확인한 뒤 전수 결과에 반영.
5. 승인된 첫 글의 작업 기준을 통과하면 다음 순서 #55·#79·#103을 재검토하여 순차 이관. 장기 자동 스케줄은 수집 실패·누락·과탐지 검사를 먼저 완성한 후 적용.

## 2차 구현·검증 후속 기록 — 2026-09-22 12:51 KST 이후

- **단계 2:** `docs/legacy-content-priority-2026-09-22.md`에 30편 전부 P0~P3별 실제 조치와 확인/미확인을 분리했다. #101의 출처 제목과 보건복지부 링크 도착 내용 불일치를 추가 발견했고 아직 해당 공개 글을 수정하지 않았다. #55/#79/#103의 공식 출처·인용 매핑과 미검토 원고는 Git 제외 `tmp/legacy_audit_20260922/content-worker1/`에 준비했으나 3편 모두 CLI `needs_review`; #103은 목포 기관 명단 미확인.
- **단계 3:** `wordpress/mu-plugins/bloguito-legacy-toc.php`와 `wordpress/tests/legacy-toc-contract-test.php` 신규 파일, #85 표 2개 내용 보존 교정 사본, `docs/legacy-layout-rollout-2026-09-22.md` 준비. 운영 PHP 8.3 문법 검사 및 실제 공개 30편 포함 계약 검사 207개 통과 기록. 목차는 긴 구형 19편만 생성 예정이며 신규 5편/짧은 구형 6편은 원형 유지한다. 별도 준비된 구형 요약 상자 CSS와 함께 모두 **서버 미설치**.
- **단계 4:** 기존 복지 공개 글의 종료된 신청과 공식 근거가 있는 후속 기간을 별개로 검증하는 한정 날짜 정책을 코드에 반영하고 글별 대상 ID 불일치를 업데이트 시작 전에 차단했다. #137 사전 검사 `ready`. 최초 의미 검토는 행동 URL 경로명을 JSON 내부 API로 추측해 1항목 실패했으나, 실제 비로그인 Edge에서 `HTTP 200 text/html` 조회 화면과 로그인 필요 안내를 확인해 그 근거·한계를 원고에 추가했다. 다시 실행한 별도 모델 리뷰 6항목 전부 true, `issues=[]`, `editorial_cli.py check bundle.reviewed.json` `ready`/exit 0. 근거·시간·산출물은 `docs/pilot-post-137-preparation-2026-09-22.md` 후속 기록 참조. 전체 WP 상태 인벤토리·원본 저장 해시·글별 승인·운영 반영은 미완료.
- **단계 6:** `scripts/audit_legacy_posts.py`를 공개 REST 페이지마다 총수와 페이지/ID/상태 검증 및 이전 스냅샷 변경 감지, 수기 검증 레지스터가 있을 때만 기한 도래를 표시하는 단일 읽기 전용 CLI로 확장. `test_legacy_post_audit.py` 9개 통과, 실제 공개 30편 3회 조사에서 재조사 간 신규·변경·누락 0. 검증 레지스터 미제공으로 30편 `review_due`는 null, 스케줄 미활성.
- **넓은 검사:** 전체 unittest 첫 시도 236개 중 실패 2·건너뜀 1. 단일 절차에도 `STEP 1`을 요구하던 과거 테스트 기대값을 현재 편집 지침·렌더러에 맞게 교정했고 같은 전체 명령을 다시 실행해 **236개 성공(건너뜀 1, 실패 0, 종료 0)**. 활성 Python 소스 69개 컴파일 통과. 보관용 `scripts/archive/attach_remaining_thumbs.py:9`에는 실행 범위 밖 구 문법 오류 1개가 남아 전체 85개 컴파일에서는 84개 통과·1개 실패로 구분한다. Bash 문법 2개, Node 문법 3개·정책 검사 3개, Docker Compose 정적 설정, `git diff --check` 각각 통과. 실제 브라우저 360/390px 및 200% CSS 확대 근사 관측은 `docs/legacy-browser-qa-2026-09-22.md` 참조; 기본 브라우저 확대·키보드/스크린리더·운영 배포 후 UI는 미검증.
- 현재 본 작업의 공개 WordPress 게시물, 서버 플러그인, 운영 스케줄러, 삭제·게시 상태 **변경 없음**. 코드·문서는 의도한 파일만 Git에 반영하고 배포·실제 공개 수정 여부를 별도로 기록한다.

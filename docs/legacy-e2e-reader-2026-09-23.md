# Bloguito 독립 독자 실제 E2E 감사 — 2026-09-23

## 1. 범위, 시험 환경 및 판정

**실행 시점:** 2026-09-23 약 10:49~11:06 KST. `AGENTS.md:13`을 먼저 읽고, 기존 QA Edge **CDP 9241**의 이미 열려 있던 Bloguito 사이트 탭(`0D792996BECB2D163452A249FA6D15D0`)만 재사용했다. 새 브라우저·프로필·CDP 포트를 만들지 않았다. **첫 사이트 진입은** `https://lifeinfo24.org/?utm_source=bloguito_qa_agent&utm_medium=internal_test&utm_campaign=site_checks`로 시작했고, `tmp/legacy_audit_20260923/e2e-reader/homepage-utm.json` 및 `.png`에 실제 주소와 홈 화면을 저장했다. 다른 글의 일반 링크에 UTM을 추가하지 않았다. 점검 전 `docs/legacy-flu-postpublish-qa-2026-09-23.md`, `docs/legacy-semantic-link-audit-addendum-2026-09-23.md`, `docs/legacy-modernization-execution-2026-09-23.md`, `docs/legacy-operational-integrity-audit-2026-09-23.md`를 확인했다.

**PASS**는 아래 실제 브라우저 동작·대상 범위에 한정, **FAIL**은 표면상 약속한 목적지·내비게이션이 없거나 실제 다른 곳으로 도착, **UNVERIFIED**는 실제 완료를 재현하지 못하거나 검사 수단 제한이 있는 경우다. 비로그인 공개 독자 흐름만 실행했다. **개인 계정 로그인, 기관 검색 결과 입력·예약, 신청 버튼 클릭, 개인정보 입력, 환급 조회/신청 제출, WordPress 쓰기, 서버 변경, Git stage/commit/push 모두 0건.** CDP `Runtime.evaluate`는 읽기 및 QA 탭 한정 시각·포커스 조절에만 사용했다. 실제 독자 링크·폼 클릭은 CDP 마우스 이벤트, 목차는 포커스한 뒤 키보드 Enter로 시험했다. 원본 백업이나 다른 작업자의 코드/검토 파일은 변경하지 않았다.

## 2. 사이트 첫 진입 → 검색 → 글 실제 진입

홈에는 GET `form[role=search]`, `input[name=s]`, 제출 `button[type=submit]`이 존재했다. 첫 자동 시도 `reader-e2e-results.json`에서 합성 Enter를 입력했지만 URL이 홈페이지 그대로인 것을 확인했다. 이는 **테스트 드라이버가 실제 제출을 발생시키지 못한 실패**였으므로 사이트 검색 실패로 계산하지 않는다. 전용 `search-retest.mjs`는 매 건 홈페이지를 다시 열고 검색 입력을 실제 `Input.insertText`로 채운 후 **CDP 마우스로 제출 버튼 클릭** → `?s=` 결과 페이지 → 해당 제목 링크 마우스 클릭 → 목표 정식 permalink 착지를 반복했다. `search-retest.json`에 입력값·검색 URL·결과 페이지 목록·클릭 링크·도착 URL을 각 ID별 저장했다.

| 실제 검색어 | 목표 기존 공개 글 | 검사 결과 | 관찰 |
|---|---|---|---|
| `독감` | **#63** 종합 무료접종 | **PASS** | 검색 `?s=`·첫 결과 페이지에서 대상 발견·결과 앵커 클릭 후 정확한 #63 permalink 착지. |
| `어르신 독감` | **#81** 연령별 시작일 | **PASS** | 같은 과정으로 #81 정확 착지. |
| `온라인 여권` | **#140** 여권 재발급·수령 | **PASS** | 같은 과정으로 #140 정확 착지. |
| `복지혜택` | **#85** 복지멤버십·보조금24 | **PASS** | 같은 과정으로 #85 정확 착지. |
| `국세환급금` | **#127** 미수령 환급 | **PASS** | 추가 행동 링크 검사 대상도 실제 검색·클릭으로 착지. |

**결과: 5/5 검색·결과 클릭 성공.** 직접 URL 열기만으로 검색 성공을 대체하지 않았다. #85와 #127의 기존 게시 내용이 새 구조/정확한 목적지로 바뀌었다는 뜻은 아니다.

## 3. 각 글 목차·키보드·반응형 시각 확인

`reader-e2e-results.json`에서 5개 실제 사이트 글 각각 `.bloguito-article` 또는 본문 및 MU 구형 목차의 첫 `<a href="#...">`에 브라우저 포커스를 준 뒤 **CDP Enter 키 다운/업**을 수행했다. #63/#81/#140/#127은 실제 `location.hash=#step-1`, #85는 `#bloguito-legacy-85-1`로 바뀌었고 각 대응 DOM ID가 존재했다. 첫 목차 앵커의 포커스 상태도 5/5 참이다. 해당 링크 후 Tab의 다음 포커스를 기록했고, 구형 #85에서는 스크롤 가능한 표 영역 `div[role=region][tabindex=0]`으로 이동한 사실을 확인했다. **목차 키보드 Enter: 5/5 PASS.** 이 실측은 문서 전체 Tab 순서·스크린리더·모든 앵커의 하단 시각 위치 전수 인증은 아니다.

각 글을 실제 Edge에서 **360px, 390px, 1280px** 디바이스 메트릭 및 별도 1280px `document.body.style.zoom='200%'`로 렌더해 총 **5 × 4 = 20개**의 새 스크린샷을 자체 Git 무시 폴더에 저장했다(`post-{ID}-{360,390,1280,css200}.png`). 각 뷰포트의 `documentElement/body.scrollWidth - innerWidth`는 360/390에서 **0px**, 1280/CSS 200%에서는 **-15px**(브라우저 스크롤바 폭 차이)였고, 목차 1개·첫 연결 대상 존재·일반 즉시 로드 이미지 깨짐 0이었다. 모바일 실제 첫 화면 이미지에서 제목·메뉴·본문 시작이 보였고, 가로로 전체 문서가 밀려 나가는 현상은 관찰되지 않았다. **해당 스크린샷의 첫 화면 및 DOM 가로폭 한정 PASS**, 모든 아래쪽 블록에 대한 수작업 스크롤·전체 모바일 WCAG 인증은 **UNVERIFIED**다.

**#85 표 접근성 별도 관측:** 모바일 360px에서 표 **2개**, 각각 caption 있음·열 머리글 3개·행 머리글 2/5개·스크롤 영역·`tabindex=0`이었다. 첫 영역 크기 `clientWidth=300px`, `scrollWidth=580px`; 내부 스크롤바와 ‘좌우로 밀어 확인’ 문구가 실제 보인다(`post-85-table-keyboard-360.png`). 포커스는 실제 영역에 들어갔지만 모바일 에뮬레이션에서 ArrowRight 8회 후 `scrollLeft=0`; 추가 일반 데스크톱 키보드 모드 360px에서도 `overflow-x:auto`, `client=285`, `scrollWidth=580`, 포커스 참인 상태에서 CDP `rawKeyDown`과 `keyDown` 각각의 ArrowRight 후 `scrollLeft=0`이었다. 이어 휠 이벤트는 CDP 호출 timeout으로 종료됐고, 네이티브 물리 키보드/터치 스와이프 및 보조기술로 교차 검증하지 못했다. 따라서 **표의 정적 접근성 구조 PASS / 실제 화살표 스크롤 동작 UNVERIFIED(재현 의심 있음)**. 이 관측만으로 사이트 CSS/JS가 키보드 스크롤을 확정 차단한다고 주장하지 않는다. 물리 키보드로 첫 영역 Tab 진입 → ArrowRight → 실제 `scrollLeft`/오른쪽 열 표시 확인이 재현되면 그때 접근성 버그로 등록해야 한다.

**브라우저 고유(native) 200% 확대:** 별도 CDP Ctrl+`+` 네 차례 및 Ctrl+`0`을 시도했지만 `innerWidth=1256`, `devicePixelRatio=1`, `visualViewport.scale=1` 전후 변화가 없어 실제 Edge UI 배율이 변경됐다는 증거가 없다(`additional-results.json`). **native 200% = UNVERIFIED**. 위 스크린샷은 명시적으로 **CSS zoom 200% 근사**이며 네이티브 200%와 혼용하지 않는다. 전체 키보드/스크린리더/OS 고대비·브라우저 콘솔 상호작용까지 인증한 것은 아니다.

## 4. 주요 글의 실제 클릭·최종 경로 검증

| 출발/독자 약속 | 실브라우저 조작과 관측 | 판정·원인 |
|---|---|---|
| **#63 및 #81** `인플루엔자 지정의료기관 검색 ↗` | 각 글에서 실제 CTA 버튼을 별도 클릭. 둘 다 새 탭의 정확한 `https://nip.kdca.go.kr/irhp/mngm/goVcntMngm.do?menuCd=333`으로 도착. 공식 질병관리청 예방접종도우미 문구·**2026~2027절기 인플루엔자 국가예방접종**, 어린이/어르신/임신부 탭, 시·도 등 검색 필터가 실제 렌더됨. `reader-e2e-results.json`의 `actions.63/.81`, `destination-{63,81}-kdca.png`. | **PASS (CTA 실제 공식 검색 화면 착지)**. 기존 직접 `requests` TLS 오류는 이 Edge 환경의 동작 실패를 뜻하지 않았다. 실제 기관별 검색 실행·재고/예약은 **UNVERIFIED**. |
| **#81 → 관련 종합 글 #63** | 검색으로 #63은 실제 접근 가능하고 기존 독립 링크 감사에서 `?p=63` GET 200·정식 permalink 도착 검증. 그러나 #81 본문 내 `?p=63`/정식 permalink 앵커 0, 전체 실제 일반 페이지 `<a href>`도 **38개 중 관련 링크 0**. `reader-e2e-results.json`의 `actions.81.relatedLinks=[]`, 기존 `legacy-semantic-link-audit-addendum:17-19`. | **FAIL: 보조 독자 내비게이션 회귀**. 옛 ‘어린이·임신부 출생일/접종 횟수는 종합 글에서’ 관련 링크가 개정 시 빠짐. KDCA 병원 CTA는 목적이 달라 대체할 수 없음. #81 어르신 본문 자체의 일정·준비물 답변은 별도로 남아 있으므로 전체 접종 사실 오류라고 과대평가하지 않음. |
| **#127 ‘국세청 차세대 모바일’ 문헌 링크** | 이 링크는 `.bloguito-cta` **0개**, 실제 `ul.source-list a`이고 href는 `https://mob.tbht.hometax.go.kr/jsonAction.do?actionId=UTBRDAAA03F001`. 글에서 실제 링크를 클릭하자 같은 탭이 **`https://hometax.go.kr/websquare/websquare.html?w2xPath=/ui/pp/index_pp.xml`**로 전환·제목 ‘국세청 홈택스’. 이후 별도 재방문 뒤 5.5초 대기 화면은 홈택스 브랜드·통합 검색창과 회전 로딩 표시였고 직접 미수령 환급금 조회 폼/완료는 확인되지 않음. `actions.127.hometax`, `additional-results.json`, `destination-127-hometax-after-wait.png`. | **FAIL (독자가 기대할 수 있는 직접 환급 조회 경로가 아님)**, 로그인 후 실제 환급 서비스 동작은 **UNVERIFIED**. 목적지의 공식 리디렉션/동적 로딩과 원고 링크의 ‘직접 조회’ 기대가 불일치하는 것이 관측 원인; Site Kit/WordPress 필수 스크립트 실패로 추정하지 않음. 원고는 현재 CTA 버튼이 아닌 출처 링크라는 사실도 보존. |
| **#140 정부24 신청·상태 조회** | 현재 글 `.bloguito-cta` **0**, gov.kr 계열 본문 앵커도 **0**, ‘정부24에서 여권 재발급 검색’, ‘여권발급 상태 조회’는 텍스트로만 안내. **기존 글에 있던 정부24 루트 링크는 현재 클릭 불가능**. 기사 밖에서 수동으로 `https://www.gov.kr/` 방문하자 `https://plus.gov.kr/` **메인 홈페이지**로 도착. 그 홈페이지의 **실제 ‘여권 재발급’ 링크를 마우스로 클릭**하자 `https://www.gov.kr/portal/service/serviceInfo/126200000030` 제목 ‘여권 재발급 온라인 신청 | 민원 안내 및 신청 | 정부24’와 대상/불가 조건 및 ‘신청하기’ 버튼이 보였고, 별도 `.../126200000037` 이력 조회·`.../126200000038` 상태 조회 문헌 링크가 있었다(`destination-followup.json`). ‘신청하기’ 클릭/로그인은 하지 않음. | **FAIL: 본문에서 직접 공식 업무 링크가 소실**, 텍스트 절차는 보존. 새 직접 서비스 URL 후보는 실브라우저에서 확인했지만 기초 출처/버튼으로 새 원고에 넣으려면 독립 근거·검토·CAS 필요. 정부24 루트 자체를 ‘신청하기’ URL로 복원하면 최종 화면이 틀림. |
| **#85 두 행동 안내 링크** | 구형 본문의 `[정부24] ... ‘보조금24’ 원클릭 조회 (새창)` 실제 href는 `https://www.gov.kr/`, **해당 앵커 마우스 클릭**으로 새 탭 `https://plus.gov.kr/` 일반 정부24 메인 도착. `[복지로] 맞춤형 급여안내(복지멤버십) 온라인 신청 바로가기 (새창)`의 실제 href는 `https://www.bokjiro.go.kr/`, **해당 앵커 클릭**으로 `https://www.bokjiro.go.kr/ssis-tbu/index.do` 일반 복지로 메인 도착. `welfare-link-clicks.json`. | **FAIL 2건: ‘원클릭 조회’·‘온라인 신청 바로가기’라는 독자 약속과 실제 일반 홈페이지 도착 불일치.** 정확한 개인 혜택·가입 서비스는 로그인이 필요할 수 있으나 실제 서비스 화면/인증 경계는 이 루트 URL로 검증되지 않았다. #85 본문 수정은 현재 다른 출처·기간 정책 사유로 HOLD 상태이므로 코드/정책 우회·직접 WP 링크 패치 금지. |

**#140 공식 목표 구분:** `.../126200000030`은 여권 **재발급 신청 안내**, `.../126200000038`은 **발급 상태 조회 안내**, `.../126200000037`은 **발급 이력 조회 안내**로 현재 정부24 서비스 페이지의 실제 앵커에서 확인된 서로 다른 목적지다. 상태 조회·이력 조회 URL의 최종 페이지를 독립 클릭/로그인까지 완주한 것은 아니므로 `href 존재` 이상의 기능 성공으로 쓰지 않는다. #140 원고의 ‘신청’과 ‘수령 상태’를 한 개의 잘못된 홈 링크로 합치지 않는다.

## 5. 콘솔·네트워크 오염과 진짜 결함의 구분

기존 QA 사이트 탭의 `Page/Runtime/Log/Network` 이벤트를 직접 수집한 `reader-e2e-results.json`에서 총 **111개 로그 이벤트**: `log-error=17`, `loadingFailed=16`, `log-warning=78`; 메인 검사 중 `Runtime.exceptionThrown=0`, `HTTP >=400 responseReceived=0`. `loadingFailed` **16개 모두** 요청 URL이 `https://lifeinfo24.org/wp-content/plugins/wp-statistics/assets/js/tracker.js?ver=14.16.14`, 오류 `net::ERR_BLOCKED_BY_CLIENT`, `blockedReason=other`였다. 17개 콘솔 error도 이 동일 추적 스크립트의 차단 메시지. 이는 **브라우저 측 차단된 분석용 리소스**이지 글 본문/검색/KDCA 이동 필수 자산 실패 증거는 아니다. 정확히 어떤 확장 프로그램이 차단했는지는 확장 목록·필터 규칙을 조사하지 않아 **UNVERIFIED**; 서버 HTTP 404나 워드프레스 자체 런타임 오류로 둔갑시키지 않는다.

78개 warning 중 **76개**는 반복되는 `Permissions-Policy` 헤더의 현재 Edge가 인식하지 못하는 기능 키 4종(`attribution-reporting`, `private-aggregation`, `join-ad-interest-group`, `run-ad-auction`), **나머지 2개**는 외부 홈택스 `Deprecated API for given entry type`. 전자는 호환성 경고이며 검색/목차/실제 CTA 성공을 막는 사이트 크래시로 관찰되지 않았다. 후자는 제3자 목적지 경고로 Bloguito의 콘솔 결함에 포함하지 않는다. analytics tracker 차단이 실제 통계 기록에 미치는 영향은 별도 분석 필요하며 이 검사는 광고 차단 확장 설정을 변경하거나 보안 필터를 해제하지 않았다.

## 6. 좁은 원인·수정 권고와 재현 절차 (실행하지 않음)

1. **#81 내부 관련 링크:** 게시된 v3의 의미상 보존 테스트에 관련 글 링크 회귀 검사 항목이 없어 전체 6/6 리뷰를 통과해도 원본 `?p=63` 내비게이션이 사라졌다. `legacy-semantic-link-audit-addendum-2026-09-23.md:26-29`의 최소 수정안대로 #81 어르신 정보 말미에 **관련 글 전용 앵커**로 #63 종합 글을 소개할 것. 내부 링크를 공식 `sources`나 의료기관 `actions`에 위장하거나 원시 HTML 수동 WP 삽입 불가. 지원 렌더/스키마가 없다면 해당 소유자가 좁은 구현·회귀 추가, 새 전체 공식 근거 결합→독립 의미 6/6→정규 review/check→실 WP 전체 재고·원본 백업/SHA/CAS→ID81 한정 업데이트→이번 검색·목차·실제 관련 링크 클릭 재실행. **현재 v3에 리뷰 없이 링크만 붙이는 것은 금지.**
2. **#85 두 루트 CTA:** 본문 앵커의 표시 문구와 `href` 계약이 실제 맞지 않는 **콘텐츠 목적지 선택 결함**. 복지로 복지멤버십 신청, 정부24 ‘나의 혜택’/보조금24 조회의 고유 공식 URL과 익명 접근·로그인 경계를 따로 실브라우저로 확인하고 서비스 종류/자격을 그대로 설명하는 새 구조화 후보를 만들 것. 단순 루트 주소→미확인 URL 치환, 로그인 우회, 날짜 정책의 evergreen 위장 금지. #85 독립 출처·기간 차단부터 해결하고 전체 재검토를 거칠 것.
3. **#127 홈택스:** 실제 `mob.tbht...jsonAction`이 홈택스 일반 경로로 전환되므로 페이지 내부 정확한 미수령 환급 조회 URL 또는 공식 검색·메뉴 순서를 **비로그인 최종 화면까지 재현**해 실용적 안내를 제안. 확인 못 하면 직접 조회 CTA로 표현하지 말고 일반 홈택스 진입과 후속 메뉴 경로를 명시. 개인 계정 조회·제출은 별도 권한이 없다면 테스트하지 않음. #127은 구조 최신형이어도 목적지 품질 PASS가 아님.
4. **#140 여권:** 기사에서 빠진 정부24 링크 편의를 원한다면 위 공식 서비스 안내 `.../126200000030`, 상태 조회 `.../126200000038`의 서로 다른 목적을 새 출처·행동 링크로 검증하고, 정확한 안내·레이블·로그인 전 한계·본문 상호 일치성을 심사한 뒤 정규 글별 갱신. 루트 `https://www.gov.kr/`을 직접 신청/상태조회로 표시해 되돌리는 회귀를 만들지 않음.
5. **접근성·운영:** #85 표 화살표키는 물리 키보드 및 터치로 재현 후 실제 차단을 판단. 새 글의 **네이티브 200%**, 전체 문서 스크롤·Tab 순서/보조기술/브라우저 뒤로가기·실제 외부 서비스 개인 성공은 이 결과에서 **UNVERIFIED**. 분석 스크립트 차단은 읽기 동작 실패의 근본 원인으로 간주하지 말고 독립 추적 여부를 별도 진단.

**재현 명령과 근거 경계:** 기존 QA Edge가 이미 `9241`에서 열려 있을 때만 Git 무시 폴더의 `browser-probe.mjs`(UTM), `reader-e2e.mjs`(초기 폭/CTA/네트워크), `search-retest.mjs`(제출 오류 수정 및 5/5 검색), `additional-checks.mjs`(#85/정부24/홈택스), `destination-followup.mjs`(정부24 실제 서비스 클릭), `welfare-link-clicks.mjs`(#85 실제 두 링크)를 사용했다. `keyboard-hometax-recheck.mjs`는 표 휠 CDP timeout이 발생해 **그 뒤 홈택스 재시도 단계는 미실행**; 성공 자료로 취급하지 않는다. `reader-e2e.mjs` 두 번째 반복에서도 캡처 timeout이 발생해 중간 결과가 없었고, 검색 결과는 별도 `search-retest.json`의 완료 기록으로만 판정한다. 별도 브라우저를 열어 실패를 가리지 않았다. 외부 탭은 실제 CTA 클릭으로만 4개 생성됐으며 `cleanup-tabs.mjs`에서 해당 정확한 4 ID만 `Target.closeTarget` 성공 확인, 원래 QA UTM 탭 및 기존 Edge 내부 탭 보존(`cleanup-tabs.json`).

**최종 공개 상태 재확인:** 11:05:58 KST 읽기 전용 공개 REST 5건 `final-public-links.json`: #63/#81/#85/#127/#140 모두 HTTP200·`publish`, 기존 공개 수정시각 유지. #81 내부 `?p=63` 없음, #63/#81 공식 검색 CTA 있음, #85 정부24 루트 href 존재, #127 모바일 홈택스 URL 존재, #140 gov.kr href 없음. 이것은 REST의 최종 시점 스냅샷이고 그 뒤의 동시 변경까지 보증하지 않는다.

**최종 상태:** 검색·목차·대상 360/390/1280/CSS200 정적 범위 PASS; **#81 내부 링크 회귀 FAIL 1**, **#85 루트 행동 링크 목적지 FAIL 2**, **#127 직접 환급 조회 목적지 FAIL 1**, **#140 직접 정부24 행동 링크 부재 FAIL 1**. 정규 글 3편의 운영 저장본·메타 무결성에 대한 기존 보고와 모순되지 않는 **독자 행동 계층 결함**이다. 네이티브 확대·실로그인 서비스 실행·#85 물리 키보드 화살표는 UNVERIFIED로 남겼다. 이 감사로 WordPress/운영 서버/다른 작업자 소스·테스트를 변경하지 않았다. 새 추적 문서는 **이 문서 하나**, 실행 스크립트/JSON/스크린샷은 본인 Git 무시 `tmp/legacy_audit_20260923/e2e-reader/`에만 둔다.

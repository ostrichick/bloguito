# Bloguito 기존 공개 글 현대화 — 6단계 구현 독립 통합 QA (2026-09-23)

## 결론 및 판정 범위

**전체 6단계 계획은 미완료(PARTIAL)이며, 검증된 기존 공개 글 3편의 적용과 반복 감사 보존 결함의 로컬 수정·재검증은 확인됐다.** 2026-09-22 최초 공개 30편(새 구조 5/구형 25)과 달리, 9/23 #77이 별도 사유 미확인으로 휴지통에 있어 당일 적용 직전 공개 29편(새 구조 13/구형 16)이었다. 기존 공개 글 #63·#81·#140의 갱신 후 공개 **29편=새 구조 16+구형 13**, 신규 글 작성 0이다. #77은 현대화 실적이 아니며 복원·삭제하지 않았다. 검증된 사전·사후 공개 REST 29편 비교에서 변경 ID는 정확히 `[63,81,140]`, 필드는 각 `content,excerpt,modified`뿐이고 나머지 26편은 관측 필드가 동일하다. 이 결과는 해당 스냅샷 사이의 공개 REST 불변성이지 이후 모든 시간·모든 서버 자산의 보증은 아니다.

판정: `PASS`는 아래에 적은 구체 검사 통과만, `PARTIAL`은 계획 일부 달성이나 필수 작업 잔여, `HOLD/FAIL`은 근거 충돌 또는 명시 요구 미충족, `UNVERIFIED`는 검증 증거 부족이다. 원래 요구의 "전수 현대화"를 후보 생성이나 분량 증가만으로 완료 처리하지 않는다. 확인된 사실·추론·미검증은 항목별로 구분한다. 정본은 `AGENTS.md`, `docs/EDITORIAL_SYSTEM.md`, `agent-publisher/editorial_policy.json`, `docs/legacy-post-modernization-worklog-2026-09-22.md`, `docs/legacy-content-priority-2026-09-22.md`, `docs/legacy-modernization-execution-2026-09-23.md`다. 후속 실행 전 작성된 승인 대시보드의 "운영 수정 0건"은 현재 상태가 아니다.

## 1. 원래 계획 대 완료 기준 교차 검증

| 단계 / 완료 기준 | 판정 | 확인된 구현·누락·범위 판단 |
|---|---|---|
| 1. 공개·비공개 전수 재고와 실제 본문 진단 | **PASS(재고·저장 지문 한정), 전체 사실 진단 UNVERIFIED** | 공개 REST 29개 완전 수집, WP 정상 39개(공개29·초안10), 휴지통6(#77 포함) = 전체 고유 45개. 9/22 저장 지문 40개 대비 #77만 정상 목록 제외, 공통39 중 본문 변경 `[63,81,140,218]`; #218은 9/22 17:58 별도 CTA 후속 변경. 다른35 본문·제목·상태 동일. 비공개 제목·본문은 출력하지 않았다. 29편 전부의 실제 행동 링크·공식 사실을 오늘 사람 검증했다는 증거는 없음. |
| 2. P0~P3 위험·계절·독자 가치 우선순위 | **PARTIAL** | 9/22 공개30편의 최초 P0~P3 전수 분류 문서 존재. 오늘 최신 구조 16/잔여13 목록과 각 보류 이유는 갱신. #77 및 이미 개선된 글을 여전히 미수정 P0로 세면 오류. 오늘 13편 대상의 검색 실측·유입·수익을 반영한 새로운 정량 우선순위의 완료 증거는 없음. |
| 3. 공통 목차·콜아웃·표·접근성 | **PARTIAL** | 운영 MU 세 파일의 9/22 최종 SHA 재확인 3/3, #85 실공개 HTML에서 표2·caption2·열 머리6·행 머리7·스크롤 초점 영역2, 목차1. 아래 별도 Edge 12개 화면 표본에서 가로 문서 넘침 0·깨진 목차 해시 0. 다만 브라우저 확대는 CDP `setPageScaleFactor(2)` 근사이지 네이티브 200%/키보드 전체 탐색/스크린리더 인증은 아님. 기존 MU로 구형 글의 DB 원고가 자동 현대화된 것은 아님. |
| 4. 대표 시범과 검토·업데이터·사후 확인 | **PASS(실제 갱신 3편에 한정)** | 전일 #137 시범 기록 및 9/23 #63/#81/#140의 공식 원문 재조회, 독립 6/6 의미검토, 정책 digest·본문 digest, CLI check, 사전 인벤토리·저장 SHA/백업·CAS 후 정규 updater 적용 기록 확인. 사후 실제 DB `post_content` SHA가 세 새 검토본과 정확 일치, 제목·slug·게시일·공개 상태 보존, REST/공개 페이지 HTTP200. v1/v2 독감 패키지와 최종 v3를 혼동하지 않았다. 아래 #81 탐색 링크 회귀는 별도 FAIL. |
| 5. 나머지 글 출처·원고·개별 갱신 | **PARTIAL / 게시 준비 0편** | 남은 구형13 `#70,#85,#99,#103,#119,#125,#139,#144,#145,#163,#217,#219,#220` 전부 정규 `editorial_cli.py check` **13/13 exit 2**, 새 독립 리뷰 0/13. 계절 5편 `review`도 5/5 exit 2(독립 모델 호출 전 차단). 출처 내부 SHA·인용 형식 16/16 일치만으로 공식 현행성·실제 적용 조건을 증명하지 않는다. 우회 게이트·가짜 기한·임의 evergreen 불허. |
| 6. 반복 감사·수정 기록·지속 운영 | **PARTIAL** | 공개 REST 기준선/차이 감지 및 09:16 수동 recurring 1회 성공; 개별 실행 문서와 독립 보고서 존재. 보존 정리의 실제 삭제 버그 발견→수정→동일 재현 재검증 성공(아래). 그러나 legacy audit 운영 cron 0, 알림 발송/수신 0 또는 미검증, 사람 review register 미제공으로 공개 29편의 검토 기한 미등록. 기존 무표식 런은 안전상 자동 정리를 보류한다. |

## 2. 데이터·게시 무결성과 콘텐츠 검증

- **확인:** 9/23 당일 새 게시 ID 0, 공개 ID 추가·이탈 0, 세 글 외 공개26편 비교 대상 필드 전부 동일. #63 DB 저장 SHA `128f680d38734c1fcc4f7edcd40cf96e186632092665d9a3999330014568a541`, #81 `959d932cb488e8c85685ef37333478635ac6d0bb29d819985b469d83fbb71c3f`, #140 `9a0d8c8b1db3b1bd9fcf93bdde1781ce6bdb35fd97d38a9919c58df16404f1b1`. 공개 REST의 렌더 SHA와 DB 원문 SHA는 해시 대상이 다르다. 세 원본 백업과 각 manifest의 원본 해시도 3/3 일치. 9/22 개정 11편의 오늘 DB 저장 SHA는 개별 완료 기록과 11/11 동일.
- **확인:** 16개 후보 내 보존 공식 `source.text` SHA와 인용 연결은 16/16 일치했으나 오늘 공식 고유 URL 33개 재조회 중 파서 성공31, 기존 SHA 동일21·변동10, 접근/파서 실패2(NOL 403 및 HWP). 변동 중 실시간 오피넷 가격은 단순 카운터 잡음으로 무시하면 안 된다. 오늘 승인 가능한 잔여 글 **0**.
- **구체 HOLD:** #103 목포시 보건소 자체사업 `15~65세`와 시 보도자료 `15~64세` 충돌, 국가기관 81곳과 어린이 예시 6곳/시 자체사업 적용범위 별개. #70/#99 현재 공연 판매·상세 NOL 403 및 날짜/좌석 미인증. #85 welfare/evergreen 정책과 과거 사업별 조건표·신청 체크리스트 제거 위험. #119/#144 9/30, #145/#163/#217/#219 9/27의 잔여 7/4일은 현행 30일 게이트 미달; 정책 명시 예외는 #225 한 건뿐. #125/139 중복·NTS `’25` 코호트 파서 및 공식 출처 변동, #139 실제 WP 제목 불일치. #220 신청 종료일 공식 근거 부재. 후보 #70/#85/#99 본문은 과거 원문 대비 약 절반 축소되어 행동 조건 소실 여부를 독립 대조해야 한다. 원고 자동 통과를 위한 조건 삭제·조작은 금지.
- **별도 실제 결손:** 이미 최신형인 #127의 공식 출처 목록에 홈택스 링크1은 있지만 명시적 직접 조회 CTA는 **0**. HTTP200은 로그인 후 조회 폼 성공이 아니다. #304의 전자레인지 단일품목 vs 일반 크기·다량 기준 공식 해석 충돌은 미해소. 공개 #77 휴지통 이동 사유는 모름.
- **추론과 한계:** #140의 일반 `gov.kr` 홈 링크가 없어졌지만 새 본문은 정부24 검색·여권 상태 조회 메뉴를 구체적으로 안내하고 외교부 공식 출처는 유지했다. 안전한 신청 직행 URL은 인증되지 않았으므로 단순 문자열 복원이나 CTA 임의 생성 금지. #63/#81의 질병청 일반 포털 링크는 지정기관 검색 CTA로 목적이 구체화됐고 9/16 공식 PDF도 유지. 다만 검색 서비스 실제 종료 화면은 TLS 재협상 오류로 end-to-end 미검증.

### #81 내부 관련 글 탐색 회귀 — **실제 확인된 FAIL, 운영 수정은 HOLD**

원본 #81은 어린이·임신부 일정을 설명하면서 출생일과 2회 접종 여부 등 종합 일정용 내부 `https://lifeinfo24.org/?p=63` 링크를 제공했다. 최종 검토/운영 #81의 본문 전체에는 동일·동등한 내부 안내가 없다. #63 링크 자체는 현재 HTTP200으로 해당 종합 글로 연결된다. 질병청 지정기관 검색 CTA나 출처 PDF는 **다른 사용자 과제**이므로 이를 내부 관련 글의 대체라고 주장할 수 없다. 어르신 접종이라는 #81의 중심 답변 오류를 새로 확인한 것은 아니지만, 부가적인 교차 주제 탐색은 손상됐다. 정확한 원본·검토 SHA 및 살아 있는 앵커 3/3 교차 검증: `docs/legacy-semantic-link-audit-addendum-2026-09-23.md`. 그 문서에 복원 문단/링크 제안이 있으나 **아직 적용되지 않았다**. 구조화 원고에 관련 링크 필드가 없는 상황에서 검토된 v3 HTML을 수동 편집하면 리뷰 SHA 무효: 새 근거·원고→독립 의미 6/6→CLI review/check→최신 WP inventory·백업/CAS→단일 ID updater→사후 브라우저 QA를 먼저 통과해야 한다.

## 3. 실제 기술 검사, 결함 원인·수정·동일 재검사

| 검사 | 실제 결과 | 엄격한 범위 |
|---|---|---|
| Python 전체 단위테스트 | 결함 발견 전 **292 실행, 291 PASS·1 SKIP**; 버그 패치 후 **296 실행, 295 PASS·1 SKIP, 실패0/exit0** | 현 dirty 작업 트리 및 신규 4개 회귀테스트 포함. `technical-qa/retention-full-unittest.log` 등. CI 원격 실행 결과는 별도 미확인. |
| 감사 보존 함수 집중 테스트 | **18/18 PASS**, 신규 가짜 sentinel·정상 3런 보존2·손상 manifest/해시/추가파일·링크 경계 포함 | 전용 `tempfile`만 사용. Windows 실제 symlink 생성은 권한 오류로 모의 분기 검사만 수행. |
| 완전히 동일한 삭제 결함 재현 | 수정 전 2회 `FORGED_DIR_REMOVED True`, 정리1; 수정 후 **2회 `FORGED_DIR_REMOVED False`, 정리 None/경보·checkpoint 유효**, exit0 | 증거 `technical-qa/reproduce_audit_boundaries.py`, `boundary-probe*.log`, `retention-repro-after-{1,2}.log`. 실제 운영 상태에서 파괴 테스트 실행 안 함. |
| 구문/정적 | 활성 Python **88/88** compile 통과, Git Bash 백업·복원 2개 `bash -n`, Node 2개 `node --check`, bridge npm test **3/3**, Compose `config --quiet`, `git diff --check` 모두 exit0 | PHP CLI·ShellCheck 부재로 직접 실행 불가. 비활성 `scripts/archive/attach_remaining_thumbs.py:9`는 별도 기존 SyntaxError; 직접 WP 미디어 수정 위험이 있어 실행/수정하지 않음. |
| 출판 사실 패턴 | `scripts/audit_published_facts.py --strict` exit0·탐지0 | 제한적 알려진 패턴만 탐지. 29편 전체 사실/링크 검증 완료가 아님. |

**삭제 결함의 원인과 실제 조치:** 기존 `scripts/audit_legacy_posts.py::_prune_owned_runs`는 실행 ID 모양과 `public-rest-snapshot.json` 존재만 보고 `shutil.rmtree`했다. 악의/무관 디렉터리의 sentinel까지 삭제되는 로컬 실증으로 문서의 "도구 생성 완료본만 정리" 계약 위반을 확인했다. 소유 worker가 `scripts/audit_legacy_posts.py`와 `agent-publisher/tests/test_legacy_post_audit.py`만 좁게 수정했다. 이제 run 생성 완료/원자 이름 전환 전 schema·site·run ID·UTC 시각·불변 스냅샷/triage/report의 저장 바이트 SHA를 `run-manifest.json`에 기록하고, 정리 전 **모든** 실행 폴더의 구조·정확한 5파일·링크/junction·해시·공개 snapshot·report/alert 대응을 확인한다. 한 곳이라도 모르면 삭제0·`retention_warning`/`retention_requires_attention`. 정상 서명 3회·retain2에서는 정확히 오래된 정상 1개만 정리했다. 변동 가능한 `alert-summary.json`은 잘못 불변 해시로 묶지 않고 구조로 검사한다.

**남은 보존 운영 경계:** 09:16 이전 실제 로컬 수동 감사 실행본 `20260923T001638Z-b2cf243a`에는 새 manifest가 없으므로 소급 소유 확인 없이 삭제 불가. 다음 retain 시 경고·자동 삭제 보류가 안전 동작이며, 운영자의 별도 검증/이관 전 cron 활성화 금지. manifest는 로컬 일관성 증빙이지 디지털 서명·경쟁 중 파일 교체에 대한 완전 방어는 아니다. 실제 Linux symlink/junction 공격·동시 교체 검사는 미실시.

## 4. 추가로 회복하여 실행한 Edge 실브라우저 표본

이전 포트 9237 연결 실패 후 **새 브라우저를 반복 생성하지 않고 이미 열린 단일 QA Edge/CDP 9241 세션을 재사용**했다. `tmp/legacy_audit_20260923/prime/live_browser_readonly_qa.py`의 일시적인 문서 전환 JS 예외는 로딩 중 execution context 변경이 원인이며 재시도 조건을 좁혀 동일 스크립트를 정상 완료했다. 프로젝트 소스 실패가 아니라 임시 QA 실행 경계였다. 규정 첫 방문 UTM 확인 `True`; #63/#81/#140 및 구형 #85 각각 요청 360·390·1280px = **12/12 로드 완료**, 문서 가로 스크롤 넘침 **0/12**, 목차 해시 대상 소실 **0/12**. #85의 실제 표 2·caption2·열 머리6·행 머리7도 관측. 390px 화면 4장 `live-browser-evidence/post-{63,81,140,85}-390.png`, 구조·오류 원자료 `live-browser-evidence/results.json`. #85 표의 개별 요소는 가로 스크롤 영역을 의도하므로 문서 전체 overflow 0과 구별한다.

390px에서 CDP `Emulation.setPageScaleFactor(2)` 결과 4/4 scale=2, 문서 폭 지표 초과0. **이것은 실제 브라우저 설정의 200% 텍스트 확대/스크린리더 시험이 아니다.** 네 글 모두 콘솔에 `Failed to load resource: net::ERR_BLOCKED_BY_CLIENT` 1개씩 표시(12/12 뷰포트). 차단된 자산 URL·필수 기능 영향이 식별되지 않아 "콘솔 오류0"이라고 기록하지 않는다. 레이아웃 검사 결과만 제한 PASS, 차단 리소스 원인·수동 키보드 이동·스크린리더·네이티브 200%·로그인 후 업무 완료는 **UNVERIFIED**. 화면 캡처와 공개 GET 외 WordPress 쓰기 없음.

## 5. 백업·운영·기존 작업 보존

- 독립 운영 읽기 전용: 04:00 v3 백업 36,540,891바이트, mode600, SHA `f8d8892685f6527496172681b1e9c173693a2cb188da79d0b937c3d9ba7d97b3`, `restore_backup.sh --verify-only` exit0/`VERIFY_ONLY_OK`. **실제 격리 복원/암호화·오프사이트 복구는 미검증**. 기존 구성에 비암호화 압축 비밀 포함 가능성이 지적됐으므로 외부 공유 금지.
- 운영 MU 3/3 동일 SHA·저장 본문/메타 예상치 일치. cron 목록엔 04시 백업과 08시 미확정 업무 엔트리2개가 있지만 별도 legacy audit cron은 0; 알림 전송·수신 경로와 사람 review register 미완료. 배포·cron 변경은 이 감사에서 수행하지 않았다.
- 시작 Git HEAD `979dfac`/`main...origin/main`, 이미 추적 수정 14개와 다수 다른 작업자 미추적 산출물이 존재. 결함 수정은 지정된 코드·테스트 2개와 기술 담당자 문서, 각 독립 담당자는 **자신의 새 보고서만** 추가했다. prime의 이번 추적 추가는 **현재 이 통합 MD 하나뿐**, 브라우저 QA 코드·증거는 기존 Git 제외 `tmp/legacy_audit_20260923/prime/`만. `git diff --check` exit0. `git reset`, 광범위 stage/commit/push, 실제 서버/WordPress 재갱신·복원·삭제 0. 기존 변경의 상세 비교는 시점별 14개 추적 수정 개수와 담당자 파일 범위 기준이고, 모든 미추적 파일의 바이트 동일성을 전수 인증했다는 뜻은 아니다.

## 6. 완료 불가 항목 및 정확한 다음 승인 단위

1. **게시물:** 남은 13편은 각각 공식 적용 대상·판매/신청/마감·중복·최소 30일을 해결하고, 원고 축소로 빠진 조건을 검토해야 한다. 새 source SHA와 본문 전체 의미 6/6·CLI check, 현재 WP 저장 SHA와 메타 CAS/원본 백업/한 글 업데이트·사후 QA 통과 전 발행 금지. #103·#304 공식 충돌이나 #220 기한을 추정값으로 채우지 않는다.
2. **운영 회귀:** #81 내부 관련 글 탐색 링크는 실제 소실 확인. 제한된 문단/정규 렌더링 경로를 별도 수정 대상으로 삼되 현 리뷰 digest 무단 변경 금지. #127 직접 조회 CTA는 목적지 인증 후 별개 수정 대상으로 관리. #140 일반 정부24 링크의 편의성 감소는 실신청 목적지 확정 전 자동 복원하지 않는다.
3. **운영 자동화:** 이전 4파일 무표식 감사 실행본의 보존·수동 이관 정책을 먼저 결정하고 Linux에서 실제 링크 경계를 추가 검증한 후, 별도 승인으로 주기 cron·경보 수신·검토 등록부·오프사이트 암호화 및 격리 복원 훈련을 진행한다. 이 보고서는 자동 설치나 실복구 완료 승인이 아니다.

**세부 독립 증거:** `docs/legacy-plan-compliance-audit-2026-09-23.md`, `docs/legacy-technical-regression-audit-2026-09-23.md`(최종 §6가 수정 후 결과), `docs/legacy-operational-integrity-audit-2026-09-23.md`, `docs/legacy-content-regression-audit-2026-09-23.md`, `docs/legacy-semantic-link-audit-addendum-2026-09-23.md`. 일시점별 기계 원자료는 각 문서의 `tmp/legacy_audit_20260923/{technical-qa,ops-qa,content-qa,seasonal2,prime}/`에 Git 제외 보관. 비공개 백업/초안 내용·계정 비밀은 이 보고서에 인용하지 않는다.

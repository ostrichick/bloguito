# 공개 게시물 #137: 게시 전 승인 패키지 — 2026-09-22 13:00 KST

**상태: 로컬 준비 및 출처·검토 검사 통과 / 게시물 변경 미실시 / 글별 승인 대기.**

## 실제 운영 WordPress에서 읽기 전용으로 확보한 원본

- 대상: 게시물 **ID 137**, 상태 `publish`. 제목 `2026년 상반기 근로장려금 신청 종료 후 지급일·심사 조회: 12월 17일 예정`.
- 본문의 **DB 저장 원본** SHA256: `4ad2b39e55b9b483bd7e5b1c1c70613d5965df7ec88ed74194923925fb0dad7b`. 기존 공개 REST의 `content.rendered` SHA와 다르므로 후자는 절대로 갱신 명령에 전달하지 않는다.
- 로컬 원본 전체 JSON 백업: `tmp/legacy_audit_20260922/pilot137/approval-20260922-1/post-original.PRIVATE.json` (Git 제외, 공개 배포 금지). 파일 SHA256 `6e01fb2f0f2a965a7487a8cbd60b92c1be3e66dc8bc95bd5ad7af034f5e5cfb6`. 백업에 운영 글의 원본 전체 필드가 포함되므로 공개 저장소에 추가하지 않는다.
- **두 번째 로컬 사본**: `backups/preapproval/2026-09-22/post137-original-1300.PRIVATE.json`에도 동일 원본을 덮어쓰기 없이 복사했고 두 파일 SHA256이 위 값과 일치함을 확인했다. `backups/`는 Git 제외다. 두 로컬 복제본의 존재는 별도 물리 저장장치·운영 DB 복구 성공을 증명하지 않는다.
- 실제 저장 슬러그·제목·공개 상태·게시일(2026-09-17 16:25:57)·최근 수정시각(2026-09-21 09:01:59)은 `approval-manifest.json`에 기록했다. 현재 발췌문은 비어 있어 검토 원고의 첫 단락으로 새 발췌문을 넣는 것이 전용 갱신 경로의 예상 동작이다. 기존 수동 작성 발췌문이 생기면 이를 보존하도록 코드가 작동한다.
- `ssh`로 WordPress의 공개/임시/대기/예약/비공개 **전체 게시물 상태 목록**과 #137의 개별 원본을 *읽기만* 하고 ID·상태·제목·저장 본문 일치 여부를 대조했다. 다른 게시물의 비공개 원문은 패키지 보고서에 기록하지 않았으며, 비교 검사의 입력으로만 사용했다.

## 변경 사항: 독자에게 직접 제공하는 정보

| 기존 본문 | 검토한 변경안 |
| --- | --- |
| 9월 1~15일 신청 종료는 알리지만, 신청을 놓친 사람에게 이후 신청기간을 국세청·상담센터에서 확인하도록 안내. | 근로소득만 있는 가구의 **2027년 3월 1~15일 하반기**, **5월 1~31일 정기 신청**과 정기 신청을 놓친 경우 **11월 30일까지 기한 후 신청**을 표로 직접 명시. 정기·반기 100%, 기한 후 95%는 **심사 산정액의 지급 기준**으로 설명. |
| 12월 17일 예정과 상반기 최대 115만 원을 안내. | 법정 지급기한 12월 30일과 12월 17일 예정일을 구분하고, 연간 예상산정액의 35% 선지급·이듬해 6월 정산 및 **15만 원 미만/환수 예상 시 12월 지급 유보**를 추가. |
| 근로소득 이외 소득은 정기신청 안내 중심. | 근로소득 단독/사업·종교인소득 포함 가구를 분리하고, 단독·홑벌이·맞벌이 소득요건과 2025년 6월 1일 기준 재산, 부채 미차감·감액 조건을 공식 인용에 연결. |
| 조회 방법에 홈택스 메인 링크·메뉴 안내. | 국세청의 **반기 심사진행상황 조회 화면**으로 연결되는 행동 버튼과 메뉴 경로 제시. 비로그인 실제 브라우저에서 `HTTP 200 text/html`, 조회 화면·로그인 안내 확인; 로그인 뒤 2026년 귀속 결과는 미검증으로 구분. |

제목·슬러그·카테고리·미디어·원 게시일·공개 상태는 수정 대상이 아니다. 예상 수정 필드는 검토 HTML `post_content` 및 현재 비어 있는 `post_excerpt`뿐이다. 변경하면 WordPress 수정시각과 리비전은 시스템 동작에 따라 변할 수 있으므로 보존 보장 대상과 구분한다.

## 실제로 확인한 품질 게이트와 비교 자료

- 별도 AI 의미 검토의 **6개 항목 전부 `true`, issues=[]**. 리뷰 본문 `tmp/legacy_audit_20260922/pilot137/bundle.reviewed.json`; 공식 근거 3개는 국세청의 현재 페이지를 직접 재조회해 모든 해시 일치.
- 이 패키지를 만들 때 전 상태 인벤토리를 사용해 `validate_bundle()`을 다시 실행: `ready`, 사유 0. 원문 재조회해 기록한 SHA도 검토 원고와 동일. `approval-manifest.json`의 `preflight_status=ready`·`source_hashes_match_live=true`가 그 결과이다.
- **원본(저장 본문) 21개 구조화 텍스트 블록 → 검토안 43개 블록**, 개정 HTML 10개 표 데이터 행. 차이점은 `content-diff.txt`(66줄), 전체 정적 비교는 `compare-preview.html`, 완성 HTML은 `proposed-reviewed.html`에 있다. 세 파일 모두 `tmp/legacy_audit_20260922/pilot137/approval-20260922-1/` 아래이며 Git 제외다.
- 브라우저 검사 기록: `docs/legacy-browser-qa-2026-09-22.md`. 공개 **기존** #137의 360/390px 여백·목차 문제를 확인한 결과로, 변경안의 운영 테마 적용 결과를 증명하지 않는다. 정적 HTML QA와 실제 배포 후 QA는 구분한다.
- 실행 재현: `./agent-publisher/.venv/Scripts/python.exe -B scripts/prepare_post_approval.py --post-id 137 --bundle tmp/legacy_audit_20260922/pilot137/bundle.reviewed.json --out-dir tmp/legacy_audit_20260922/pilot137/approval-<새-빈-폴더>` (읽기 전용 서버 조회와 로컬 파일 작성만 수행). 기존 승인 폴더가 있으면 덮어쓰지 않는다. 스크립트 전용 검사는 `test_post_approval_preflight.py` 7건 통과.

## 승인 경계 및 복구 방법

**이번 작업에서 승인 플래그를 전달하거나 WordPress `post update`를 실행하지 않았다.** 사람이 #137의 개정 내용과 미리보기를 보고 이 **특정 글**을 승인한 다음에만 다음 절차를 진행한다.

1. 적용 당일 새 빈 폴더에 승인 패키지를 재생성한다. 정확히 같은 ID·원문 SHA·제목·출처 해시·독립 검토 유효성(`review_max_age_hours=24`)·전체 상태 인벤토리를 다시 확인한다. 하나라도 변하면 승인 직전 검토로 되돌린다.
2. 별도 안전한 보관 위치에 `post-original.PRIVATE.json` 원본을 추가로 보존하고 SHA를 검증한다. 독립 백업은 WordPress 운영 전체 복원 성공을 뜻하지 않는다.
3. **게시물 ID 137에 대한 명시적 승인 이후에만** 정규 경로 `editorial_cli.py update-existing bundle.reviewed.json --post-id 137 --expected-content-sha256 <당일 원본 저장 SHA> --confirm-update`를 사용한다. 스크립트는 원본 백업 후 본문·필요한 발췌문만 수정하고 저장 결과를 재조회한다. 직접 WP-CLI/PHP로 편집 검증을 건너뛰지 않는다.
4. 라이브 URL에서 제목·슬러그·게시일·카테고리·미디어·본문/발췌문, 모든 표·목차·조회 링크 및 360/390px·실제 브라우저 기본 200% 확대와 키보드 조작을 검증한다. 실패하면 **추가 갱신을 중단**한다.
5. 되돌려야 하면 먼저 현재 WordPress 본문이 *방금 적용한 검토 HTML의 해시*와 동일한지 확인하여 그 사이의 다른 편집을 보존한다. 승인된 운영 복구 절차에서 원본 백업의 `post_content`·원본 발췌문만 복원하고 ID·제목·슬러그·게시일·상태가 유지됐는지 재조회한다. 이 문서는 복구를 실행하지 않았으며, 별도 변경이 발견되면 원본을 무조건 덮어쓰지 않는다.

**참고:** 현재 준비는 게시 직전 검토가 가능하다는 뜻이지 영구 승인이나 운영 배포 완료가 아니다. 다음 날에는 근거·검토의 24시간 제한에 따라 재조회·재검토가 요구될 수 있다.

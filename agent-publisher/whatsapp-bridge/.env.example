# Comma-separated WhatsApp owner identifiers (numeric JID or LID part before @).
# Place actual values only in the ignored .env and chmod 600 before enabling the service.
BLOGUITO_WHATSAPP_OWNER_IDS=YOUR_OWNER_ID

# Numeric IDs of other authorized owners (JID or LID part before @), separated by commas.
# Your own account is authorized only in its self-chat; messages you send to other chats are ignored.
# Place actual IDs only in ignored .env (chmod 600) before enabling external owner commands.
BLOGUITO_WHATSAPP_OWNER_IDS=YOUR_OWNER_ID
